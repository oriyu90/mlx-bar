# MLXBar v1.5.3 Test Plan

v1.5.3は、公開APIの認証前リソース消費を塞ぎ、生成中のトークン毎秒をメニューバーへ表示するリリースです。

## 受入基準

### 認証前リソース消費

1. 認証情報のない要求は、本文を解析する前にHTTP 401で拒否される。
2. 壊れたJSONでも認証なしなら401になる（422にならない＝本文が先に解析されていない）。
3. `/health`は認証なしで200を返す。
4. `requireToken: false`の構成では従来どおり通過する。
5. `Content-Length`が上限を超える要求は、本文を転送せずHTTP 413で拒否される。
6. 設定が許す最大の正当な要求（画像8枚×25MiBのbase64＝約280MB）は事前拒否されない。
7. `Transfer-Encoding: chunked`では受信量が上限に達した時点で打ち切られ、メモリが上限に比例して収まる。
8. 設定ファイルが不完全でも、要求ごとに走るこの判定が500にならない。
9. 認証失敗がAPIログへ`AUTHENTICATION_FAILED`として残る。
10. 同時接続数が`api.maxConcurrentConnections`で頭打ちになる。

### 生成中のトークン毎秒

11. `GET /api/v1/status`に`generationTokensPerSecond`と`generatedTokens`が出る。
12. テキストを出さないトークンが続く生成でも計測される。
13. ランタイムが`generation_tps`を提供しない場合、Worker側の実測へ切り替わる。
14. ランタイムの最初の無意味なサンプル（数万トークン毎秒）が公開されない。
15. 生成終了後は該当フィールドが`status`から消える（古い値が残らない）。
16. 進捗がOpenAI応答の本文に混入せず、keep-aliveとして扱われる。
17. 進捗イベントが既存のtool_parse heartbeatを妨げない。
18. `status()`が想定外の内部状態でも例外を出さない（毎秒ポーリングされるため）。

## 実機・障害注入シナリオ

- 隔離インスタンスへ認証なしの大きな要求を並列投入し、RSSを実測する。
- 生バイトを送らず`Content-Length`だけを宣言して上限判定を確認する。
- チャンク送信で上限の数十倍を流し、上限値を変えてRSS増加が追従することを確認する。
- 実モデルで生成しながら`status`を秒間隔で観測し、値の推移と終了後の消滅を確認する。
- ランタイムのカウンタを持たないadapterと、無意味な初回サンプルを返すadapterを注入する。

## 完了条件

- Pythonテストが非推奨警告なしで完走する。
- Swift Debug／Release buildが成功する。
- DMG内のアプリ版が1.5.3、build 22で検証される。
- 公開assetのSHA-256がローカル成果物と一致する。

## 2026-08-22 実施結果

- 認証なし40MB×24並列: RSS 60 → **69 MB**（v1.5.2は 60 → 1,822 MB）。
- 認証なしPOST→401、壊れたJSON（認証なし）→401、`/health`→200、`requireToken:false`→通過。
- `Content-Length: 400000000` → 本文を転送せず413。`Content-Length: 279000000`（正当な最大相当）は事前拒否されず本文待ちに入った。導出上限は281,068,842バイト。
- チャンク419MB送信: 上限281MBでRSS +707MB、上限10MBで **+33MB**。増加分はアプリ側で、上限がそのまま効く。
- 認証失敗25件が`AUTHENTICATION_FAILED`としてAPIログに記録された。
- 実機`Qwen3.5-9B-MLX-8bit`は111トークンを14秒ため込んでから1つの`delta`で吐く。この条件でも進捗が毎秒発火し（25→58→91→123→156トークン、32〜34 tok/s）、`status`へ反映され、生成終了後はフィールドごと消えた。OpenAI応答本文への混入0件。
- Pythonテスト212件が成功。Swift Debug／Release buildが成功。

## 既知の範囲

- 上限は「設定が許す最大の正当な要求」に合わせるため、既定では約281MBと大きい。認証済みクライアントは上限×同時接続数までメモリを要求できる。画像入力を使わない場合は`api.maxRequestBytes`を小さくすることで下げられる。
- チャンク送信の打ち切りはHTTP 413ではなく、本文が途中で終わったことによる422として現れる。メモリが上限で収まることが目的であり、エラーコードの一致までは保証しない。
- トークン毎秒はランタイムの累積平均のため滑らかで、速度低下への反応は緩やかである。
