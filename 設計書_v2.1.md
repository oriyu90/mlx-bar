# MLX LM / VLM メニューバーアプリ 設計書

| 項目 | 内容 |
|---|---|
| 文書版 | 2.1 |
| 仕様確認日 | 2026-08-09 |
| 対象 | Apple Silicon Mac |
| 配布方針 | Developer ID 署名・公証による直接配布 |
| 位置づけ | 実装開始時の設計・仕様・受け入れ基準 |

> 本書は旧「MLX-VLM メニューバーアプリ 設計書」を分析し、MLX LM対応、将来のMLX LM / MLX VLM更新、LM Studioモデル連携、可変ポート、メニューバーGUI操作を追加した改訂版である。

---

## 0. 本レビューでの変更点（v2.0 → v2.1）

全体設計は妥当だが、実装時に詰まりやすい箇所を是正・詳細化した。

| 区分 | 対象 | 内容 |
|---|---|---|
| 是正 | 9.3 | ポート切替を「デュアルリスナー方式」に一本化し、再起動ベースの手順を例外時のフォールバックに格下げ。進行中ストリームのdrain処理を追加 |
| 是正 | 5.2〜5.3 | Login Item helperの独自監視・独自IPCをやめ、launchd（<code>SMAppService</code>）のKeepAliveに委ねる方式へ変更。管理系IPCを1系統に統一 |
| 是正 | 18 | 「常駐時CPU 1%未満」の前提となるhealth polling間隔を明記し、数値目標の根拠を担保 |
| 詳細化 | 7.4（新設） | 生成キャンセルの実装方式と、prefill中でyieldポイントがない場合の扱いを明記 |
| 詳細化 | 5.1、8.1 | 「全体でロード済み1モデル」の制約がLM Studio自身のロード状態には及ばないことを明記 |
| 詳細化 | 6.3 | MLX VLM判定プローブの具体的な検査項目（vision関連キー、preprocessor_config等）を明記 |

---

## 1. 目的

Apple Silicon Mac上で、テキスト生成モデルとマルチモーダルモデルをメニューバーから一元管理し、GUI、CLI、ローカルAPIのどの入口からでも同じ状態を安全に操作できるアプリを提供する。

対象エンジンは次の3系統とする。

1. <code>mlx-lm</code>: MLX形式の言語モデル
2. <code>mlx-vlm</code>: MLX形式の画像・音声・動画対応モデル
3. LM Studio Provider: LM Studioが管理するモデルをLM StudioのローカルAPI経由で利用

アプリは上流ライブラリのAPIをGUIから直接呼ばない。安定したアプリ内契約と、上流差分を吸収するエンジンアダプターを間に置く。これにより、MLX LMとMLX VLMを個別に更新でき、更新後に互換性が崩れた場合は旧ランタイムへ即時ロールバックできるようにする。

---

## 2. 旧設計の分析

### 2.1 維持する設計

- Swift / SwiftUIによるネイティブなメニューバーアプリ
- GUIとCLIが同じバックエンド状態を共有する構成
- モデルは原則1つだけロードし、Apple Siliconのユニファイドメモリを保護
- localhostまたはUNIX Domain Socketに限定したローカル通信
- 推論、ダウンロード、更新ログのストリーミング表示
- モデル状態とバックエンド状態の明示的な状態遷移

### 2.2 改修が必要な点

| 旧設計 | 問題 | 改修方針 |
|---|---|---|
| <code>mlx-vlm</code>だけをバックエンド内でimport | LMを明示的に選べず、依存関係更新が全体に波及する | MLX LM / MLX VLMを別ワーカープロセス・別仮想環境に分離 |
| 1つのvenvをその場でpip upgrade | 更新中に現用環境が壊れる。freezeからの復元も完全ではない | 新スロットへ解決・インストール・試験後、原子的に切替 |
| HFキャッシュまたは独自フォルダのみ | LM Studioで設定変更したモデル保存先を扱えない | <code>lms ls --json</code>とユーザー選択フォルダをモデルソース化 |
| 形式判定がない | GGUFをMLXへ直接渡すなどの誤ロードが起こる | MLX / GGUF / 不明を事前分類し、利用可能エンジンを限定 |
| ポート設定が抽象的 | 競合時、変更時、GUI再接続時の動作が未定義 | 管理用UDSと公開API用TCPを分離し、GUIから検証・適用 |
| 進捗を常に百分率で返す想定 | 上流APIが正確なロード率を公開しない場合がある | フェーズ、処理中メッセージ、取得可能な場合だけ百分率を返す |
| FastAPI自体がモデルを保持 | 更新・クラッシュ・メモリ解放の影響範囲が大きい | コーディネーターはMLXをimportせず、ワーカーを監督 |
| APIと上流サーバー仕様が混在 | 上流変更がそのままGUI/CLI破壊につながる | アプリ固有管理APIとOpenAI互換推論APIを明確に分離 |

### 2.3 重要な制約

- MLX LM / MLX VLMはMLX互換のモデルディレクトリをローカルパスからロードできる。
- LM Studioの標準モデルフォルダは <code>~/.lmstudio/models/publisher/model/...</code> だが、保存先はLM Studioの設定で変更できるため、固定値だけに依存してはならない。
- LM StudioはGGUFとMLXの両方を扱える。GGUFはMLX LM / MLX VLMへ直接ロードせず、LM Studio Providerへルーティングする。
- 上流のCLI、Python API、レスポンスフィールドは将来変更され得る。「無条件に最新版へ追従」ではなく「互換性試験に合格した版だけ有効化する」ことを将来対応の定義とする。
- <code>mlx-lm</code>の付属HTTPサーバーは公式文書上、基本的なセキュリティ確認のみで本番用途非推奨である。本アプリは上流サーバーを外部公開せず、独自コーディネーターで認証とバインド制御を行う。

### 2.4 要件トレーサビリティ

| ユーザー要望 | 主設計 | GUI操作 | 受け入れ基準 |
|---|---|---|---|
| MLX LM対応 | 5、6、7、12 | 11.2〜11.4 | 21.1 |
| MLX VLM対応継続 | 5、6、7、12 | 11.2〜11.4 | 21.2 |
| 将来版を独力で簡単に更新 | 7.3、10 | 10.6、11.2 | 21.5 |
| LM Studioモデルフォルダのモデルを利用 | 6、8 | 11.2、11.3、11.5 | 21.3 |
| ポート番号を変更 | 9、15 | 9.2、11.2、11.5 | 21.4 |
| 上記をメニューバーGUIで操作 | 11 | メニュー、各管理画面 | 21.1〜21.5 |

---

## 3. スコープ

### 3.1 MVPで作るもの

- メニューバー常駐、状態表示、モデル選択、ロード、アンロード
- MLX LMとMLX VLMのテキストストリーミング推論
- MLX VLMへの画像入力。対応モデルでは複数画像を許可
- Hugging Faceキャッシュ、任意のモデルフォルダ、LM Studioモデルフォルダの統合一覧
- LM Studioモデルフォルダの自動候補提示、手動選択、再スキャン
- MLX形式モデルの直接ロード
- GGUFを含むLM Studio管理モデルのLM Studio Provider経由利用
- MLX LM / MLX VLMの個別更新、バージョン指定、更新履歴、ロールバック
- 公開APIのポート番号変更、競合検査、適用、復旧
- GUIと同等のCLI
- OpenAI互換の <code>/v1/models</code> と <code>/v1/chat/completions</code>
- localhost限定、APIトークン、ログの秘密情報マスキング

### 3.2 MVP後の拡張

- <code>/v1/responses</code>の完全互換範囲拡大
- 音声・動画入力、TTS、STT、画像生成
- LoRAアダプター、draft model、KVキャッシュ詳細設定
- 複数モデル同時ロードと連続バッチ
- Hugging Face検索・ダウンロードGUI
- Shortcuts.app、グローバルショートカット、リモート端末連携

### 3.3 作らないもの

- モデルの学習、ファインチューニング、量子化
- GGUFをMLX形式へ自動変換
- 任意のLAN公開を初期状態で有効にする機能
- App Store配布
- root権限の要求、<code>sysctl</code>の自動変更
- 上流の破壊的変更を試験なしで自動承認する仕組み

---

## 4. 技術上の決定

| 領域 | 採用 | 理由 |
|---|---|---|
| GUI | Swift 6、SwiftUI、<code>MenuBarExtra</code> | ネイティブ、低常駐負荷、設定画面とメニューを統合しやすい |
| 最低OS | macOS 14、macOS 15推奨 | MenuBarExtra利用。大規模MLXモデルの最適化はmacOS 15以降を推奨 |
| 管理サービス | Python 3.12、FastAPI、uvicorn | GUI/CLI共通の型付き管理APIとSSEを実装 |
| ランタイム管理 | 同梱した固定版 <code>uv</code> | Pythonと環境をアプリ専用領域で再現可能に管理 |
| MLX LMワーカー | 専用venv + <code>mlx-lm</code>アダプター | VLMと依存関係・更新周期を分離 |
| MLX VLMワーカー | 専用venv + <code>mlx-vlm</code>アダプター | LMと独立して更新・ロールバック可能 |
| LM Studio連携 | <code>lms</code> CLI + LM Studio REST/OpenAI互換API | 現在のモデル保存先とロード状態をLM Studio側の契約で取得 |
| 内部IPC | UNIX Domain Socket | ポート変更中もGUI/CLIの管理接続を失わない |
| 公開推論API | 既定 <code>127.0.0.1</code>、任意 <code>0.0.0.0</code> TCP | OpenAI互換クライアントから利用可能。LAN公開中はAPIキー必須 |
| 状態保存 | SQLite + JSON設定 | 履歴・ジョブはSQLite、手動復旧可能な設定はJSON |
| ログイン起動 | <code>SMAppService</code> | 現行macOSのログイン項目APIを利用 |

配布物に含めるPythonとuvはコード署名時に固定する。アプリ本体のバンドル内は更新時に書き換えず、ダウンロードしたランタイムはApplication Support配下に保存する。

---

## 5. 全体アーキテクチャ

~~~text
┌──────────────────────────┐       ┌──────────────────────────┐
│ Menu Bar App             │       │ mlxbarctl                │
│ SwiftUI                  │       │ CLI                      │
└────────────┬─────────────┘       └────────────┬─────────────┘
             └──────────────┬────────────────────┘
                            │ 管理用UDS
                            ▼
              ┌───────────────────────────────┐
              │ Coordinator                  │
              │ FastAPI / 状態・認証・ルート選択 │
              │ MLXをimportしない             │
              └──────┬──────────┬─────────────┘
                     │          │
        ワーカーUDS   │          │ HTTP / lms CLI
          ┌──────────┘          └──────────────┐
          ▼                                    ▼
┌─────────────────────┐           ┌─────────────────────┐
│ MLX LM Worker       │           │ LM Studio Provider  │
│ runtime/mlx-lm      │           │ localhost:1234等    │
└─────────────────────┘           └─────────────────────┘
          ▲
          │ 別ワーカーUDS
┌─────────────────────┐
│ MLX VLM Worker      │
│ runtime/mlx-vlm     │
└─────────────────────┘

Coordinator ── 127.0.0.1:<可変ポート> ── OpenAI互換クライアント
~~~

### 5.1 プロセス分離の原則

- コーディネーターはモデルオブジェクトを保持しない。
- 各MLXワーカーは1回に1モデルだけを保持する。
- モデル切替時は通常のアンロードを試し、メモリが戻らない場合はワーカープロセスを終了・再生成する。
- MLX LMとMLX VLMは同時起動できるが、MVPでは「全体でロード済み1モデル」を既定とする。この制約はMLX LM / MLX VLMワーカーにのみ適用し、LM Studio自身がロードしているモデルはLM Studioプロセスが独自に管理するため制御対象外とする。両方に同時にモデルがロードされている場合、ユニファイドメモリを二重に消費し得るため、GUIはLM Studio Provider有効時にメモリ逼迫の警告を表示するが、ロードをブロックはしない。
- 更新対象のワーカーだけを停止する。他方のランタイムスロットは変更しない。
- ワーカーのクラッシュはコーディネーターが検知し、最大2回まで指数バックオフで再起動する。再発時は <code>Failed</code> として自動再起動を止める。

### 5.2 GUI・CLI・サービスの役割分担

- GUIは表示、入力検証、ユーザー確認、ジョブ進捗表示を担当し、モデルやvenvを直接操作しない。
- CLIは自動化用の薄いクライアントであり、GUIと同じ管理APIだけを使う。設定ファイルやSQLiteを直接変更しない。
- Coordinatorは状態の唯一の書込み主体であり、排他、認証、設定、ジョブ、ルーティングを担当する。
- Coordinatorのプロセス起動・生存監視・クラッシュ時再起動はlaunchdに委ね、GUIやCLIが独自の監視ループを実装しない。GUI終了時にサービスを継続するかはGeneral設定に従う。
- サービス停止中にGUIを開いた場合はオンデマンド起動する。CLIは暗黙に起動せず、<code>--start</code>指定時だけlaunchd経由で起動を依頼する。

### 5.3 Coordinatorの起動方式

CoordinatorはGUIアプリの子プロセスとしてではなく、<code>SMAppService.agent(plistName:)</code>で登録する独立したLaunchAgentとして起動する。GUIプロセスがCoordinatorを直接<code>Process</code>で生成・監視する実装は、GUI終了時の意図しない巻き添え終了や、独自の再起動ロジックの複雑化・不具合を招きやすいため避ける。

- GUI終了後もサービスを継続する設定の場合、Coordinatorの生死はGUIプロセスに紐づかないため、launchdの管理下でそのまま起動し続ける。
- launchdのplistで<code>KeepAlive</code>を有効にし、異常終了時の再起動はSwift側の監視コードではなくlaunchdに任せる。再起動が既定回数（例: 10分間に5回）を超えた場合はlaunchdがバックオフし、GUIはhealth check失敗としてエラー表示する。
- CLIの<code>--start</code>は独自のIPCプロトコルを新設せず、<code>launchctl kickstart -k gui/$(id -u)/<label></code>相当の処理（またはSwift側の<code>SMAppService</code> start API）を呼び出すことで実現する。これにより管理系IPCは管理用UDS 1系統のみに保たれる。
- GUIを使わずCLIとバックグラウンドサービスだけで運用する構成も、この方式であれば追加設計なしに成立する。

---

## 6. モデルカタログ設計

### 6.1 モデルソース

| source | 取得方法 | 書込み |
|---|---|---|
| <code>huggingface_cache</code> | <code>huggingface_hub</code>のキャッシュ走査 | MVPでは読取のみ |
| <code>custom_folder</code> | ユーザーがGUIで追加したフォルダ | 読取のみ |
| <code>lm_studio_folder</code> | <code>lms ls --json</code>および選択済みフォルダの走査 | 読取のみ |
| <code>lm_studio_api</code> | <code>GET /v1/models</code>またはLM Studio native API | LM Studio APIの契約に従う |

LM Studioモデル一覧は、まず <code>lms ls --json</code> を使用する。これはLM Studioの「My Models」で設定した現在の保存先を反映する。CLIがない、出力互換性試験に失敗する、または実パスが得られない場合は、GUIで保存先を選択させる。初回候補として <code>~/.lmstudio/models</code> を提示するが固定しない。

### 6.2 フォルダアクセス

- GUIの <code>NSOpenPanel</code> でフォルダを選択する。
- App Sandboxを将来有効化できるよう、選択結果はsecurity-scoped bookmarkとして保存する。
- シンボリックリンクは解決後の実パスを記録し、循環リンクを無視する。
- モデルファイルは移動、変更、削除しない。
- FSEventsで変更を監視し、2秒のデバウンス後に差分スキャンする。
- 「今すぐ再スキャン」操作をメニューバーGUIとCLIの両方に設ける。

### 6.3 形式判定

判定は拡張子だけで確定せず、ディレクトリ単位で次を検査する。

| 判定 | 最低条件 | 利用先 |
|---|---|---|
| <code>mlx_lm</code> | <code>config.json</code>、tokenizer関連ファイル、1個以上の <code>*.safetensors</code>、既知またはプローブ可能なtext architecture | MLX LM |
| <code>mlx_vlm</code> | 上記に加え、vision/audio/video設定またはMLX VLMプローブ成功 | MLX VLM |
| <code>lmstudio_mlx</code> | LM Studioカタログが <code>compatibility_type=mlx</code> と返す | 直接MLXまたはLM Studio Provider |
| <code>gguf</code> | 1個以上の <code>*.gguf</code> | LM Studio Providerのみ |
| <code>unknown</code> | 条件不足、破損、未対応architecture | 一覧表示のみ。ロード不可 |

<code>mlx_vlm</code>判定のプローブでは、<code>config.json</code>内に<code>vision_config</code>、<code>vision_tower</code>、<code>image_token_id</code>等のvision関連キーが存在するかを静的に確認し、加えて<code>preprocessor_config.json</code>または<code>processor_config.json</code>の有無を確認する。これらの静的検査だけで確定させず、実際に<code>mlx_vlm</code>側の設定ロード関数を一度呼び出せるかまでプローブし、例外が発生した場合は<code>mlx_lm</code>またはunknownへフォールバックする。

形式判定結果には <code>confidence</code> と <code>reason</code> を保存する。誤判定時はGUIで利用エンジンを上書きできるが、ロード前プローブは省略しない。

### 6.4 正規化モデルID

同じモデルがHFキャッシュとLM Studioフォルダの両方に存在しても衝突しないよう、内部IDを次の形式にする。

~~~text
<source>:<sha256(real_path または provider_model_key の先頭16桁)>
~~~

表示名、publisher、repo、variant、量子化、形式、実パス、対応モダリティ、推奨エンジン、最終検証結果を別フィールドとして保持する。APIやCLIは内部IDを使用し、実パスを外部へ露出しない。

### 6.5 エンジン選択ルール

1. ユーザーがモデルごとに指定したエンジン
2. MLX VLMと判定され、画像等の入力がある場合はMLX VLM
3. MLX LMと判定された場合はMLX LM
4. LM StudioカタログにありProviderが利用可能な場合はLM Studio Provider
5. それ以外はロード不可

MLX VLMがtext-onlyモデルを扱える版であっても、MVPではユーザーがMLX VLMを選んだ場合だけ使用する。MLX LMの独立更新・比較可能性を維持するためである。

---

## 7. MLX LM / MLX VLMアダプター

### 7.1 安定内部契約

上流Python APIはワーカー内のアダプターだけが呼ぶ。コーディネーターとの契約は次のメッセージに固定する。

~~~json
{
  "protocol_version": 1,
  "request_id": "uuid",
  "method": "load|unload|generate|cancel|capabilities|health",
  "params": {}
}
~~~

ストリームイベント:

~~~json
{"type":"phase","name":"loading_weights","message":"モデルを読み込み中"}
{"type":"delta","text":"生成された断片"}
{"type":"metrics","prompt_tps":123.4,"generation_tps":20.1,"peak_memory_bytes":123}
{"type":"completed","finish_reason":"stop"}
{"type":"error","code":"MODEL_INCOMPATIBLE","message":"...","retryable":false}
~~~

### 7.2 アダプター責務

- 上流の <code>load</code>、<code>stream_generate</code> 等を内部契約へ変換
- tokenizer / processorのchat template適用
- テキスト、画像、将来の音声・動画入力の能力検査
- cancellationの協調停止。停止できない場合はワーカー再起動
- <code>trust_remote_code</code>の既定拒否とモデル単位の明示許可
- MLXのactive、peak、cache memory取得
- 参照破棄、<code>gc.collect()</code>、<code>mlx.core.clear_cache()</code>
- 上流例外を安定したアプリエラーコードへ変換

### 7.3 capability probe

各ランタイムスロット作成時に、実際にインストールされたモジュールから能力を検査し、次をJSONで保存する。

- package version、Python version、MLX version
- import可能な関数とシグネチャ
- CLIエントリーポイントと <code>--help</code> の終了コード
- local pathロード対応
- streaming対応
- 対応モダリティ
- server機能は参考情報として記録するが、アプリ内契約には直接依存しない

未知のメジャー版または必須能力欠落時は <code>incompatible</code> とし、現用化しない。

### 7.4 cancellationの実装詳細

MLX LM / MLX VLMの生成はトークン単位で結果をyieldするジェネレータとして実装されているため、アダプターは各トークン生成後にキャンセルフラグを確認し、立っていれば生成ループを終了して <code>{"type":"completed","finish_reason":"cancelled"}</code> を返す。この方式はトークン生成フェーズでは確実に機能する。

一方、長いプロンプトのprefill（プロンプト処理）フェーズは実装によっては1回の呼び出しで完了し、その間はyieldポイントが存在しない場合がある。この間にキャンセル要求が来た場合は、既定で最大5秒待ってから応答がなければワーカープロセスを終了・再生成する。7.1の <code>cancel</code> メソッドがタイムアウトした場合の既定動作としてこれを明記する。GUI/CLIには「キャンセル処理中」から「キャンセル完了（強制終了）」への状態遷移を表示する。

---

## 8. LM Studio Provider

### 8.1 目的

LM Studioモデルフォルダ内のモデルを、形式に応じて次の2経路で使えるようにする。

- MLX形式: 実フォルダを読取専用でMLX LM / MLX VLMワーカーへ渡す。
- GGUFまたはLM Studio固有ランタイム形式: LM Studioにロードを依頼し、LM StudioローカルAPIへ推論を中継する。

これによりモデルの重複コピーを不要にし、「LM Studioフォルダ内にあるがMLXでは直接読めないモデル」もアプリの共通UIから使用できる。

### 8.2 接続設定

| 項目 | 既定値 |
|---|---|
| 有効 | true（LM Studio検出時） |
| Base URL | <code>http://127.0.0.1:1234</code> |
| API token | 未設定。必要時のみKeychainに保存 |
| 自動起動 | false |
| 自動ロード | true |
| タイムアウト | health 2秒、load 10分、推論は無制限ストリーム |

LM Studio側ポートも設定画面で変更できるが、これは本アプリの公開APIポートとは別項目にする。LM Studio Providerの「接続テスト」で <code>GET /v1/models</code> またはstatus APIを確認する。

### 8.3 操作

- 一覧: <code>lms ls --json</code>、API一覧を正規化
- ロード: 対応するLM Studio native APIを優先し、利用不可時は <code>lms load</code>
- アンロード: native APIまたは <code>lms unload</code>
- 推論: OpenAI互換 <code>/v1/chat/completions</code> を中継
- 状態: <code>lms ps --json</code>またはAPIから取得

CLIを呼ぶ場合はシェル文字列を組み立てず、<code>Process.executableURL</code>と引数配列を使う。モデルキーをそのままシェル解釈させない。

---

## 9. ポート変更設計

### 9.1 ポートの分離

| 通信 | 既定 | 変更 |
|---|---|---|
| GUI/CLI → Coordinator管理API | Application Support配下のUDS | 原則変更不可 |
| 外部クライアント → 本アプリ公開API | <code>127.0.0.1:11435</code> | GUI / CLIで変更可 |
| 本アプリ → LM Studio | <code>127.0.0.1:1234</code> | GUI / CLIで変更可 |
| Coordinator → MLXワーカー | ランダム名のUDS | 内部管理 |

管理APIをUDSに固定するため、公開ポートの再起動中もGUIは結果を受け取れる。

### 9.2 GUI操作

設定画面の「APIサーバー」に以下を置く。

- Host: 既定は <code>127.0.0.1</code>。利用者が警告を確認してLAN公開を有効にした場合のみ <code>0.0.0.0</code>
- Port: 数値入力、範囲1024〜65535
- 「空きポートを提案」
- 「接続をテスト」
- 「適用して再起動」
- 現在のURL、コピー操作
- 変更前ポートへ戻す

メニューバーの「APIサーバー」サブメニューには現在のURL、起動/停止、URLコピー、設定を表示する。

### 9.3 変更手順

ポート切替は同一プロセス内で新旧2つの<code>uvicorn.Server</code>インスタンスを並行稼働させる「デュアルリスナー方式」を既定手法とする。同一asyncioイベントループ上で複数のServerを個別タスクとして並行実行するのは一般的な手法であり、プロセス再起動を必要としない。旧設計の「二重リスナーが難しい場合は再起動」という条件分岐は不要な複雑さであり、以下のとおり単純化する。

1. 数値範囲、予約ポート、現在の利用状況を検証する。
2. 新ポートで仮リスナーを作成できることを確認する。
3. 設定を <code>pending</code> として保存する。
4. 新ポートで新しいAPIリスナーを起動し、health checkを実行する（旧リスナーは稼働継続）。
5. health成功後、新規接続の受付を新ポートへ切り替え、設定を <code>active</code> にする。
6. 旧リスナーは新規接続を受け付けないまま、進行中の <code>/v1/chat/completions</code> ストリーミング接続が完了するまで最大60秒drainしてから閉じる。60秒経過後も残る接続は打ち切り、クライアントには再接続を促すエラーを返す。
7. 失敗時は新リスナーを破棄し、旧リスナーと旧設定を維持する。

同一プロセスでの二重リスナー実行が実装上不可能になった場合（推論サーバーを別言語ランタイムへ置き換える等）に限り、旧ポートを設定に記録してから再起動し、5秒以内にhealth checkが成功しなければ旧設定で再起動するフォールバック手順を用いる。

### 9.4 競合時

- 自動で別ポートへ黙って変更しない。
- GUIへ使用中のポートと検出結果を表示する。
- 「次の空きポートを使用」をユーザーが選んだ場合だけ変更する。
- CLIは終了コード <code>4</code> と <code>PORT_IN_USE</code> を返す。

---

## 10. ランタイム更新設計

### 10.1 目標

- MLX LMとMLX VLMを別々に更新できる。
- 安定版、指定バージョン、上級者向けGit tag / commitを選択できる。
- 現用環境をその場で変更しない。
- 更新失敗、起動失敗、モデル互換性失敗のいずれでも1操作で戻せる。
- 将来の上流変更はcapability probeと契約テストで検出し、壊れた版を有効化しない。

### 10.2 ディレクトリ

~~~text
~/Library/Application Support/MLXBar/
├── config.json
├── state.sqlite3
├── control/
│   └── coordinator.sock
├── runtimes/
│   ├── mlx-lm/
│   │   ├── active.json
│   │   └── slots/<slot-id>/
│   │       ├── .venv/
│   │       ├── requirements.lock
│   │       ├── manifest.json
│   │       └── probe.json
│   └── mlx-vlm/
│       ├── active.json
│       └── slots/<slot-id>/...
├── logs/
└── cache/
~~~

<code>active.json</code>には現用slot、直前slot、切替時刻、検証結果を保存する。ファイルは一時ファイルへ書いて <code>rename</code> し、原子的に置換する。

### 10.3 更新チャネル

| チャネル | 入力 | 既定 |
|---|---|---|
| Stable | PyPIの安定版 | 既定 |
| Version | 例: <code>0.x.y</code> | ユーザー指定 |
| Git ref | 公式GitHubのtagまたはcommit SHA | 上級者設定でのみ表示 |
| Pre-release | PyPI pre-release | 初期状態off |

任意URLや任意リポジトリはMVPでは許可しない。Git refは公式リポジトリだけを許可し、branch名ではなく解決済みcommit SHAをmanifestに記録する。

### 10.4 更新フロー

1. 更新ロックを取得する。推論や別更新と競合する場合はキューへ入れる。
2. 現在版、候補版、Python要件、空き容量、ネットワークを確認する。
3. 新しいslotとvenvを作る。
4. 対象パッケージとアダプターをuvで解決し、lockを生成する。
5. 新slotだけへ同期する。現用slotは一切変更しない。
6. <code>uv pip check</code>、import、CLI help、capability probeを実行する。
7. 既知の小型テストモデルがローカルにある場合は、load、1トークン生成、unloadを実行する。ない場合は「モデル実試験未実施」と明示する。
8. ユーザーに変更対象の直接・間接依存バージョン差分を表示する。
9. 実行中推論の完了を待つか、ユーザー確認後にキャンセルする。
10. 対象ワーカーを停止し、<code>active.json</code>を新slotへ切り替える。
11. 新ワーカーのhealth checkと最終probeを行う。
12. 成功なら履歴へ記録。失敗なら直前slotへ自動復帰する。

### 10.5 個別更新

- MLX LM更新は <code>runtimes/mlx-lm</code> だけに新slotを作る。
- MLX VLM更新は <code>runtimes/mlx-vlm</code> だけに新slotを作る。
- 各slotはMLX本体を含む完全な依存グラフを持つため、片方の <code>mlx</code> 更新が他方へ影響しない。
- 「両方更新」は2つの独立ジョブとして解決・試験し、両方成功後に順番に切り替える。

### 10.6 GUI

「ランタイム管理」画面で次を提供する。

- MLX LM / MLX VLMごとの現在版、Python版、MLX版
- 「更新を確認」
- Stable / Version / Git refの選択
- 「ダウンロードして検証」
- 依存差分、リリースページへのリンク、必要容量
- 「今すぐ切替」
- 「前の版へ戻す」
- slot一覧と削除。現用と直前slotは削除不可
- 自動確認: 既定は週1回。自動インストールはoff

メニューバーには更新件数バッジと、エンジン別の「更新を確認」を配置する。更新の実行、ロールバック、ログ表示までGUIで完結させる。

### 10.7 保持と清掃

- 各エンジンで現用、直前、最新の検証済み予備の最大3slotを保持する。
- 削除前にslotがどの履歴から参照されているか検査する。
- ダウンロード途中のslotは24時間後に清掃候補とする。
- モデルキャッシュはランタイム更新で削除しない。

---

## 11. GUI設計

### 11.1 メニューバー状態

| 状態 | アイコン | 表示 |
|---|---|---|
| 停止 | グレー | サービス停止 |
| 起動中 | 回転インジケーター | バックエンド起動中 |
| 未ロード | 通常 | モデル未選択 |
| ロード中 | 進捗リング | フェーズとモデル名 |
| ロード済み | アクセント色 | モデル名、エンジン、メモリ |
| 生成中 | パルス | tok/s、キャンセル |
| 更新中 | 下向き矢印 | 対象エンジンと段階 |
| エラー | 警告 | 短い原因、詳細を開く |

色だけに依存せず、記号とテキストを併用する。

### 11.2 メニュー構成

~~~text
[状態] Loaded · MLX LM · model-name
[メモリ] Active 4.2 GB / Peak 5.1 GB
────────────────────────
モデルを選択...
ロード / アンロード
クイックチャット...
生成をキャンセル
────────────────────────
モデルフォルダ
  LM Studio: 接続済み
  今すぐ再スキャン
  フォルダを選択...
APIサーバー
  http://127.0.0.1:11435
  URLをコピー
  起動 / 停止
ランタイム更新
  MLX LMの更新を確認
  MLX VLMの更新を確認
────────────────────────
設定...
診断...
終了
~~~

### 11.3 モデル選択画面

- 検索、source、形式、モダリティ、互換性で絞り込み
- 各行に <code>MLX LM</code>、<code>MLX VLM</code>、<code>LM Studio</code> バッジ
- 「ロード可能」「要LM Studio起動」「未対応」の理由を表示
- 同じモデルの複数variantをまとめ、展開して選択
- ダブルクリックまたは「ロード」で実行
- ロード前に推定サイズ、利用可能メモリ、remote codeの要否を表示

### 11.4 クイックチャット

- model、engine、system prompt、temperature、max tokens
- テキスト入力、画像ドラッグ&ドロップ
- ストリーミング出力、停止、コピー、会話クリア
- モデルが入力モダリティ非対応なら送信前に無効化
- 入力画像は推論完了後に一時領域から削除

### 11.5 設定画面

1. General: ログイン時起動、通知、ログレベル
2. Models: HF cache、LM Studioフォルダ、任意フォルダ、監視
3. API Server: 本アプリのhost、port、token、起動設定
4. LM Studio: base URL、port、token、自動ロード、接続テスト
5. Runtimes: 更新チャネル、更新確認間隔、slot保持数
6. Advanced: remote code許可、KV cache、メモリ上限、診断出力

---

## 12. 管理API

管理APIはUDS経由を既定とし、公開TCPへは出さない。

| Method | Path | 内容 |
|---|---|---|
| GET | <code>/api/v1/health</code> | coordinatorの生存確認 |
| GET | <code>/api/v1/status</code> | サービス、モデル、ワーカー、ジョブ状態 |
| GET | <code>/api/v1/models</code> | 統合モデル一覧 |
| POST | <code>/api/v1/models/scan</code> | 再スキャンジョブ作成 |
| POST | <code>/api/v1/models/{id}/probe</code> | 互換性検査 |
| POST | <code>/api/v1/models/{id}/load</code> | エンジン指定または自動選択でロード |
| DELETE | <code>/api/v1/models/loaded</code> | アンロード |
| POST | <code>/api/v1/generate</code> | GUI/CLI向け生成 |
| POST | <code>/api/v1/generate/{request_id}/cancel</code> | 生成キャンセル |
| GET | <code>/api/v1/runtimes</code> | 現在版、slot、能力 |
| POST | <code>/api/v1/runtimes/{engine}/check</code> | 更新候補を取得 |
| POST | <code>/api/v1/runtimes/{engine}/stage</code> | 新slot作成・検証 |
| POST | <code>/api/v1/runtimes/{engine}/activate</code> | 検証済みslotへ切替 |
| POST | <code>/api/v1/runtimes/{engine}/rollback</code> | 直前slotへ戻す |
| GET | <code>/api/v1/settings</code> | 秘密値を除く設定 |
| PUT | <code>/api/v1/settings</code> | 検証付き設定変更 |
| POST | <code>/api/v1/settings/api-listener/test</code> | ポート検査 |
| GET | <code>/api/v1/jobs/{id}</code> | ジョブ状態 |
| GET | <code>/api/v1/jobs/{id}/events</code> | SSEイベント |
| GET | <code>/api/v1/diagnostics</code> | マスキング済み診断情報 |

変更系APIは <code>Idempotency-Key</code> を受け付ける。二重クリックやCLI再試行で同じロード・更新ジョブを重複作成しない。

### 12.1 公開推論API

MVP:

- <code>GET /v1/models</code>
- <code>POST /v1/chat/completions</code>
- <code>stream=true</code>のSSE
- OpenAI形式のテキストと <code>image_url</code>入力

管理操作、ローカル実パス、更新APIは公開推論APIに含めない。互換性のない上流固有フィールドは無視せず <code>400 UNSUPPORTED_PARAMETER</code> を返す。

---

## 13. CLI

コマンド名はVLM専用に見えない <code>mlxbarctl</code> とする。

~~~text
mlxbarctl status [--json]
mlxbarctl model list [--source lm-studio] [--json]
mlxbarctl model scan [--wait]
mlxbarctl model probe <model-id>
mlxbarctl model load <model-id> [--engine auto|mlx-lm|mlx-vlm|lm-studio]
mlxbarctl model unload
mlxbarctl generate --prompt "..." [--image path] [--json]
mlxbarctl runtime list
mlxbarctl runtime check mlx-lm|mlx-vlm
mlxbarctl runtime stage mlx-lm|mlx-vlm [--version VERSION|--git-ref SHA]
mlxbarctl runtime activate mlx-lm|mlx-vlm <slot-id>
mlxbarctl runtime rollback mlx-lm|mlx-vlm
mlxbarctl config get
mlxbarctl config set api.port 12000
mlxbarctl api test-port 12000
mlxbarctl lmstudio test
mlxbarctl diagnostics
~~~

- 既定で管理UDSを設定ファイルから発見する。
- 人間向け表示と <code>--json</code> を提供する。
- 長時間処理はジョブIDを返し、<code>--wait</code>でイベントを表示する。
- 終了コード: 0成功、2入力不正、3未起動、4競合、5互換性なし、6更新失敗、7認証失敗、8内部エラー。

---

## 14. 状態遷移

### 14.1 モデル

~~~text
Unloaded
  └─ load ─> Probing ─> Loading ─> Loaded ─> Generating
                 │          │          │          │
                 └─ fail ─> Error <────┘          ├─ complete ─> Loaded
                                      unload <────┘

Loaded ─ unload ─> Unloading ─> Unloaded
Unloadingでメモリ未解放 ─> WorkerRestarting ─> Unloaded
~~~

### 14.2 ランタイム

~~~text
Idle ─> Checking ─> Staging ─> Verifying ─> ReadyToActivate
         │             │            │
         └─────────────┴──── fail ─> Rejected

ReadyToActivate ─> Draining ─> Switching ─> Healthy
                                      │
                                      └─ fail ─> RollingBack ─> HealthyOld
~~~

### 14.3 APIリスナー

~~~text
RunningOld ─> Validating ─> StartingNew ─> HealthyNew ─> RunningNew
                   │             │
                   └──── fail ───┴──────────────> RunningOld
~~~

1つのリソースに対する状態変更は排他する。たとえば <code>Updating</code> 中の対象ワーカーへload要求が来た場合は <code>409 ENGINE_BUSY</code> と再試行目安を返す。

---

## 15. 設定スキーマ

~~~json
{
  "schemaVersion": 1,
  "api": {
    "enabled": true,
    "host": "127.0.0.1",
    "port": 11435,
    "requireToken": true
  },
  "models": {
    "watchFolders": true,
    "roots": [],
    "lmStudio": {
      "enabled": true,
      "folderBookmark": null,
      "baseUrl": "http://127.0.0.1:1234",
      "autoLoad": true
    }
  },
  "runtimes": {
    "mlx-lm": {"channel": "stable", "autoCheck": true},
    "mlx-vlm": {"channel": "stable", "autoCheck": true},
    "checkIntervalHours": 168,
    "keepSlots": 3
  },
  "security": {
    "trustRemoteCodeDefault": false,
    "allowLan": false
  }
}
~~~

- 未知フィールドは保持して書き戻し、将来版との前後互換性を高める。
- <code>schemaVersion</code>ごとに一方向migrationとバックアップを用意する。
- API token、LM Studio tokenはJSONに書かずKeychainへ保存する。
- 壊れた設定は <code>config.invalid-日時.json</code>へ退避し、安全な既定値で起動する。

---

## 16. エラー処理

| code | 状況 | GUI動作 |
|---|---|---|
| <code>MODEL_NOT_FOUND</code> | 移動・削除済み | 再スキャンを提案 |
| <code>MODEL_INCOMPATIBLE</code> | 形式またはarchitecture非対応 | 理由と利用可能Providerを表示 |
| <code>MODEL_REQUIRES_REMOTE_CODE</code> | remote codeが必要 | リスク説明後、モデル単位で許可 |
| <code>INSUFFICIENT_MEMORY</code> | メモリ不足予測またはOSエラー | context縮小、別variant、アンロードを提案 |
| <code>PORT_IN_USE</code> | ポート競合 | 空きポート提案、旧設定維持 |
| <code>LMSTUDIO_UNAVAILABLE</code> | LM Studio停止・URL不正 | 接続テスト、起動手順を表示 |
| <code>ENGINE_BUSY</code> | 更新・切替・別ロード中 | 現在ジョブと待機時間を表示 |
| <code>UPDATE_RESOLUTION_FAILED</code> | 依存解決失敗 | 現用版を維持し解決ログを表示 |
| <code>UPDATE_PROBE_FAILED</code> | 新版が内部契約を満たさない | 新slotをRejectedにして旧版維持 |
| <code>WORKER_CRASHED</code> | 推論ワーカー異常終了 | 再起動後、再発時は自動停止 |
| <code>UNLOAD_TIMEOUT</code> | メモリ解放に失敗 | ワーカー再生成で確実に解放 |

エラー本文にtoken、完全なユーザーパス、プロンプト本文を含めない。詳細ログではパスを <code>~/...</code> に短縮し、ユーザーが明示的に診断書へ含めた場合だけ完全パスを出す。

---

## 17. セキュリティ・macOS固有事項

- 公開APIは初期状態で <code>127.0.0.1</code> のみ。<code>0.0.0.0</code>は明示的なLAN公開操作でのみ選択し、APIキーを必須とする。
- 公開APIは初回起動時にランダムtokenを生成しKeychainへ保存する。GUIで再生成できる。
- UDSはユーザーだけがアクセスできる権限で作る。
- remote codeは既定off。許可時はモデルID、revision、ハッシュ、許可日時を記録する。
- 更新元はPyPIと公式GitHubに限定し、TLS、ハッシュ、lockfile、解決済みcommitを記録する。
- アプリバンドルを自己書換えしない。PythonランタイムだけをApplication Support内でスロット更新する。
- 外部プロセス実行のためMac App Storeは対象外とし、Hardened Runtime、Developer ID署名、公証を行う。
- ログイン項目は <code>SMAppService</code> を使い、ユーザーがGUIで有効化する。
- 本機能にアクセシビリティ権限は不要。グローバルショートカットを将来追加する場合も、権限不要なAPIを優先する。
- 一時画像は <code>Application Support/MLXBar/tmp</code> ではなくユーザー専用の一時ディレクトリに置き、完了・取消・異常終了後に清掃する。

---

## 18. 非機能要件

| 項目 | 基準 |
|---|---|
| GUI応答 | メインスレッド上の操作受付を100ms以内。ファイル走査、IPC、推論、更新を実行しない |
| 常駐時CPU | アイドル時平均1%未満 |
| 常駐時メモリ | GUI + Coordinator合計250MB以下。モデルワーカーを除く |
| Coordinator起動 | 初期インストールとモデルロードを除き3秒以内にhealth成功 |
| ポート変更 | 正常系で5秒以内に新URLがhealth成功 |
| キャンセル表示 | 操作後200ms以内にUIをCancellingへ更新 |
| モデル一覧 | キャッシュ済みDBは500ms以内に初期表示。再スキャンは非同期 |
| フォルダ走査 | 10,000ファイル規模で5秒を目標。内容全ハッシュは行わない |
| アンロード | 5秒以内に通常解放。超過時はワーカー終了で解放処理へ移行 |
| ログ保持 | 7日または200MBの早い方。更新履歴は削除しない |
| 更新復旧 | 新ワーカーhealth失敗から30秒以内に旧slotへ自動復帰 |
| アクセシビリティ | VoiceOverラベル、キーボード操作、色以外の状態表現 |

モデルロード時間と生成速度はモデル、入力長、Mac、上流版で変わるため固定SLAにしない。UIをブロックしないこと、進行フェーズを1秒以内に更新することを保証対象とする。

「常駐時CPU 1%未満」は次の前提のもとでの目標値とする。GUIはフォアグラウンド時5秒間隔、バックグラウンド時30秒間隔でCoordinatorへhealth pollingを行い、SSE接続中は追加pollingを行わない。ファイル監視はFSEventsのイベント駆動を用いポーリングしない。これより短い間隔のpollingを採用する場合は、この数値目標を再検討する。

---

## 19. ファイル・モジュール構成

~~~text
MLXBar/
├── App/
│   ├── MLXBarApp.swift
│   ├── MenuBar/
│   │   ├── MenuBarView.swift
│   │   └── MenuBarViewModel.swift
│   ├── Models/
│   │   ├── ModelCatalogView.swift
│   │   └── ModelDetailView.swift
│   ├── Chat/
│   │   └── QuickChatView.swift
│   ├── Runtimes/
│   │   └── RuntimeManagerView.swift
│   ├── Settings/
│   │   ├── GeneralSettingsView.swift
│   │   ├── ModelSourceSettingsView.swift
│   │   ├── APISettingsView.swift
│   │   └── LMStudioSettingsView.swift
│   ├── Services/
│   │   ├── CoordinatorClient.swift
│   │   ├── BookmarkStore.swift
│   │   └── KeychainStore.swift
│   └── Resources/
├── Coordinator/
│   ├── mlxbar/
│   │   ├── main.py
│   │   ├── api/
│   │   │   ├── management.py
│   │   │   └── openai_compat.py
│   │   ├── catalog/
│   │   │   ├── scanner.py
│   │   │   ├── classifier.py
│   │   │   └── lmstudio.py
│   │   ├── workers/
│   │   │   ├── supervisor.py
│   │   │   └── protocol.py
│   │   ├── runtimes/
│   │   │   ├── updater.py
│   │   │   ├── slots.py
│   │   │   └── probe.py
│   │   ├── settings.py
│   │   ├── jobs.py
│   │   └── errors.py
│   ├── pyproject.toml
│   └── uv.lock
├── Workers/
│   ├── common/
│   │   └── protocol.py
│   ├── mlx_lm_worker/
│   │   ├── adapter.py
│   │   └── contract_tests/
│   └── mlx_vlm_worker/
│       ├── adapter.py
│       └── contract_tests/
├── CLI/
│   └── mlxbarctl/
├── Tests/
│   ├── contract/
│   ├── integration/
│   └── fixtures/
└── Packaging/
    ├── entitlements.plist
    └── release/
~~~

アダプターと契約テストをエンジン別に置き、上流更新対応の修正範囲を限定する。

---

## 20. 実装プラン

### Phase 0: 技術検証

- LM Studioの既定・変更済みモデルフォルダで <code>lms ls --json</code> を検証
- LM Studio配下のMLX LM / VLMを実パスから読取専用ロード
- GGUFをLM Studio Provider経由でストリーミング
- CoordinatorのUDS管理APIと可変TCPリスナー
- MLX LM / VLMの別venvと最小capability probe

完了条件: 3形式（MLX LM、MLX VLM、GGUF）を同一カタログに出し、正しい経路で1回ずつ生成できる。

### Phase 1: コアバックエンド

- 設定、SQLite、ジョブ、エラーコード
- カタログ走査・形式判定・重複排除
- ワーカー監督、load/unload/generate/cancel
- OpenAI互換Chat Completions
- ポート検証・切替・ロールバック

### Phase 2: メニューバーGUIとCLI

- 状態アイコン、モデル選択、ロード/アンロード
- クイックチャット
- LM Studioフォルダ・接続設定
- APIポート設定
- <code>mlxbarctl</code>とJSON出力

### Phase 3: 安全な更新

- エンジン別slot、uv解決、lock、probe
- stage、activate、自動rollback
- GUI更新画面、履歴、slot清掃
- PyPI Stable、Version指定、公式Git commit

### Phase 4: 配布品質

- Hardened Runtime、署名、公証、SMAppService
- 障害注入、更新中強制終了、設定破損、ポート競合試験
- VoiceOver、キーボード操作、ログマスキング
- 移行・アンインストール手順

各Phaseはその受け入れテストを通してから次へ進む。更新機能をコア推論と同時に実装せず、先に固定版ランタイムで推論経路を安定させる。

---

## 21. 受け入れ基準

### 21.1 MLX LM

- [ ] MLX LM形式のローカルモデルが一覧で <code>MLX LM</code> と表示される。
- [ ] GUIで選択→ロード→テキスト送信→ストリーミング表示→アンロードが完了する。
- [ ] CLIで同じロード済み状態を参照し、生成できる。
- [ ] VLMランタイムを停止・更新してもLMの現用slotが変更されない。

### 21.2 MLX VLM

- [ ] MLX VLM形式のローカルモデルが <code>MLX VLM</code> と表示される。
- [ ] 画像ドラッグ&ドロップとプロンプトから結果をストリーミングできる。
- [ ] 画像非対応モデルでは送信前に画像操作が無効化される。
- [ ] LMランタイムを停止・更新してもVLMの現用slotが変更されない。

### 21.3 LM Studio

- [ ] LM Studioの標準モデルフォルダを候補として検出できる。
- [ ] LM Studioで保存先を変更した環境でも、<code>lms ls --json</code>またはGUI選択で一覧化できる。
- [ ] LM Studio配下のMLX形式モデルをコピーせず直接ロードできる。
- [ ] GGUFはMLXへ誤投入されず、LM Studio Providerだけが選択可能になる。
- [ ] LM Studio ProviderのURL/port/tokenをGUIで設定し、接続テストできる。
- [ ] LM Studio停止時にアプリがクラッシュせず、再接続方法を表示する。

### 21.4 ポート

- [ ] GUIから1024〜65535の空きポートを設定できる。
- [ ] 変更後、表示URLと <code>/v1/models</code>が新ポートで利用できる。
- [ ] 使用中ポートを指定すると適用されず、旧ポートが継続する。
- [ ] 不正値、競合、起動失敗のいずれでもGUI管理接続を失わない。
- [ ] ポート切替中、旧ポートで進行中の生成ストリームは即座に切断されず、drain完了（または最大60秒）まで継続する。
- [ ] CLIからもportの確認、検査、変更ができる。

### 21.5 更新と将来互換

- [ ] MLX LMだけを指定版へstageし、MLX VLMのslotに差分がない。
- [ ] MLX VLMだけを指定版へstageし、MLX LMのslotに差分がない。
- [ ] importまたは契約テストに失敗した候補版はactiveにならない。
- [ ] 新版のhealth check失敗時に30秒以内で旧slotへ自動復帰する。
- [ ] GUIから現在版、候補版、依存差分、検証結果、履歴を確認できる。
- [ ] GUIから前版へ戻せる。
- [ ] 更新中のアプリ強制終了後、次回起動で現用または直前の健全slotを選択できる。
- [ ] 自動確認をoffにでき、自動インストールは初期状態でoffである。

### 21.6 メモリ・障害

- [ ] モデル切替時、前モデルのワーカー参照が残らない。
- [ ] 通常アンロードが5秒を超えた場合、ワーカー再生成経路へ移る。
- [ ] ワーカークラッシュがGUIとCLIに同じエラーコードで通知される。
- [ ] 更新中、ロード中、生成中の競合操作が二重実行されない。
- [ ] 10,000ファイルのスキャン中もメニューバーが操作できる。

### 21.7 セキュリティ

- [ ] 初期状態でlocalhost以外から公開APIへ接続できない。
- [ ] tokenが設定JSONと通常ログに出ない。
- [ ] remote codeはユーザーの明示許可なしに実行されない。
- [ ] 診断情報にプロンプト本文と完全なユーザーパスが初期状態で含まれない。

---

## 22. テスト方針

### 22.1 単体テスト

- 形式判定、モデルID、設定migration、ポート範囲
- 上流例外からアプリエラーへの変換
- version / pre-release / Git SHAの入力検証
- tokenとパスのログマスキング

### 22.2 契約テスト

- MLX LM / VLMの各対応版に対するimport、関数シグネチャ、streamイベント
- LM Studio CLIのJSONをfixture化し、未知フィールド追加に耐えること
- LM Studio/OpenAI互換レスポンスの必須フィールドとSSE

### 22.3 統合テスト

- GUI→UDS→Coordinator→各ワーカー→ストリーム
- 同じロード状態をGUIとCLIで共有
- ポート切替前後のhealthと旧設定復帰
- FSEventsによる追加、削除、リネーム
- 更新slotのstage、activate、rollback

### 22.4 障害注入

- ダウンロード切断、ディスク満杯、PyPI応答不正
- venv作成中、切替中、lock書込み中の強制終了
- ワーカーの無応答、異常終了、メモリ未解放
- LM Studioの停止、port変更、認証有効化
- 破損 <code>config.json</code>、欠落safetensors、循環symlink

---

## 23. 未確定事項と既定判断

実装を止めないため、次を既定判断とする。変更する場合は実装開始前に決定記録を更新する。

| 項目 | 既定判断 |
|---|---|
| アプリ名 | <code>MLXBar</code>（仮） |
| 最低OS | macOS 14 |
| 既定公開port | 11435 |
| 同時ロード | 全エンジン合計1モデル |
| LM Studio自動起動 | off |
| 更新 | 週1回確認、手動stage / activate |
| remote code | off |
| LAN公開 | v1.0.9でオプトイン対応。既定OFF、APIキー必須、画面に公開状態と接続URLを表示 |
| 配布 | Developer ID直接配布 |
| モデルのコピー | 行わず元フォルダを読取 |

---

## 24. 公式仕様の参照先

仕様確認日: 2026-08-09。実装開始時およびランタイム対応版追加時に再確認する。

- [MLX LM 公式README](https://github.com/ml-explore/mlx-lm): MLX互換モデル、Python API、ストリーミング、ローカルパス/Hugging Face利用
- [MLX LM HTTP Model Server](https://github.com/ml-explore/mlx-lm/blob/main/mlx_lm/SERVER.md): OpenAI風API、既定localhost:8080、セキュリティ上の注意
- [MLX VLM 公式README](https://github.com/Blaizzy/mlx-vlm): local path対応、FastAPI server、動的ロード、画像等の入力
- [MLX メモリ管理API](https://ml-explore.github.io/mlx/): active/peak/cache memory、<code>clear_cache</code>
- [LM Studio CLI: lms](https://lmstudio.ai/docs/cli): 現在のモデル保存先を反映した一覧、load/unload、server操作
- [LM Studio: lms ls](https://lmstudio.ai/docs/cli/local-models/ls): <code>--json</code>によるローカルモデル一覧
- [LM Studio: lms load](https://lmstudio.ai/docs/cli/local-models/load): モデルロード、unload、メモリ見積り
- [LM Studio: Import Models](https://lmstudio.ai/docs/app/advanced/import-model): 標準モデルディレクトリ構造
- [LM Studio REST API Quickstart](https://lmstudio.ai/docs/developer/rest/quickstart): 既定 <code>localhost:1234</code>、認証、ローカルAPI
- [LM Studio Server Settings](https://lmstudio.ai/docs/developer/core/server/settings): port、認証、LAN公開、JITロード設定
- [LM Studio OpenAI-compatible Models](https://lmstudio.ai/docs/developer/openai-compat/models): <code>GET /v1/models</code>
- [uv公式ドキュメント](https://docs.astral.sh/uv/): Python環境、lock、sync、パッケージ単位更新
- [Apple MenuBarExtra](https://developer.apple.com/documentation/swiftui/menubarextra): SwiftUIメニューバーシーン

---

## 25. 完了の定義

本設計のMVP完了とは、受け入れ基準21.1〜21.7が自動または手動テストで合格し、次を実機で再現できる状態を指す。

1. LM Studioを含む複数ソースのモデルが一つのGUI一覧に現れる。
2. MLX LM、MLX VLM、LM Studio Providerの各経路で適切なモデルをロード・推論・アンロードできる。
3. 本アプリとLM Studioのportを混同せず、GUIから設定・検査できる。
4. MLX LM / MLX VLMを独立してstage・検証・切替・rollbackできる。
5. 失敗した更新、port変更、モデルロードが現用の健全状態を壊さない。
6. GUI、CLI、公開APIが同じモデル状態を観測する。
