# MLXBar v1.5.2 Test Plan

v1.5.2は、v1.5.1で追加したプロンプトキャッシュ再試行の判定を、失敗したモジュールではなく失敗した呼び出しへ絞る修正リリースです。

## 受入基準

1. キャッシュ巻き戻しの失敗だけが再試行対象になる。
2. 生成中の通常のモデルエラーは再試行されず、暖まったキャッシュも破棄されない。
3. 判定がランタイムの行番号に依存しない。
4. 上流ランタイムが巻き戻しを正しく回避するようになった場合、再試行は発火せず、再利用は従来どおり働く。
5. v1.5.0／v1.5.1で追加した動作を退行させない。

## 障害注入・実機シナリオ

- 実runtimeの`dispatch.py`経由で巻き戻し失敗と通常のモデルエラーを起こし、分類を比較する。
- 同一モジュール内に「trimを呼ぶ関数」と「呼ばない関数」を置き、モジュール一致では区別できないことを固定する。
- `_cache_fully_retained()`に`trim`の有無チェックを足して上流修正を再現し、枝分かれターンの結果と`reuseFailures`を確認する。

## 完了条件

- Pythonテストが非推奨警告なしで完走する。
- Swift Debug／Release buildが成功する。
- DMG内のアプリ版が1.5.2、build 21で検証される。
- 公開assetのSHA-256がローカル成果物と一致する。

## 2026-08-22 実施結果

- 実runtimeでの分類: 巻き戻し失敗=`True`、`'NoneType' object has no attribute 'config'`=`False`。v1.5.1ではどちらも`True`だった。
- 追加した回帰テストはv1.5.1の判定に対して失敗し、修正後に成功することを確認した。
- 上流修正を再現した実機`Qwen3.5-9B-MLX-8bit`で、継続ターンと枝分かれターン計3件がすべて`cache_tier=disk`（1,174 tokens再利用）で成功し、`reuseFailures`は0のままだった。
- Pythonテスト196件が成功した。

## 既知の範囲

- 判定はランタイムのソース行を読む。ソースを読めない配置では`trim`を含むAttributeErrorのみが補助判定となり、それ以外の例外型では回避策が働かず、v1.5.0以前と同じ失敗になる。
