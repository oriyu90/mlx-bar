# MLXBar v2.4.1

v2.4.1は、v2.4.0実装後の監査で検出した4件を修正するパッチリリースです。
新規エンジン機能・新規設定キーの追加はなく、既定値はすべてv2.4.0と同一です。

## 修正

- `general.logLevel` が設定項目だけあって動作に影響しない状態だったため、
  public APIサーバと管理サーバの両方へ配線しました。変更は次回サービス起動時に
  反映されます（ポート/LAN切替時にも再読込）。
- ログレベルに許容値（debug/info/warning/error）のサーバ検証を追加しました。
- メモリガード比率（再ロード後に反映）と最大同時接続数（再起動後に反映）の
  注意文を設定画面に追加しました。
- 1MB未満の要求サイズが0MB（自動）と表示される旨を注記しました。

詳細は `DESIGN_v2.4.1.md`、`TEST_PLAN_v2.4.1.md` を参照してください。

## 検証

- Python回帰テスト: 519 passed（v2.4.0の516＋新規3）
- Swift Debug / Release build: 成功
- 日英ローカライズの重複キー検査: 新規重複なし

DMG SHA-256: `2722c048b28b41bd8ceb725fd6ec1890bb1e94ab686d38cb6cba8d4fcbf11de6`

> 本ビルドはad-hoc署名でApple公証は行っていません。初回起動時は「システム設定 →
> プライバシーとセキュリティ」から起動許可が必要になる場合があります。
