# 変更履歴

このプロジェクトの主な変更を記録します。

## [1.6.0] - 2026-08-23

### 修正

- **中断した生成がプロンプトキャッシュを丸ごと破棄していた問題を修正しました。** ランタイムは再利用したキャッシュをその場で進めますが、対応するトークンIDを書き戻すのは生成が完走したときだけです。中断したターンではキャッシュだけが先に進み、安全な選択肢は破棄しかありませんでした。プロンプトのトークンIDと生成トークンを逐次記録して対応を組み直します。書き換えるのは、報告されるoffsetが「プロンプト＋生成」と一致し、対応付けを証明できるときだけです。証明できない場合と、offsetを報告しないランタイムでは従来どおり破棄します。
- **ハイブリッド構成モデルで、会話が枝分かれするたびにプロンプト全体を再計算していた問題を修正しました。** Qwen3.5/3.8など再帰層を含むモデルのキャッシュは短いprefixへ巻き戻せません。mlx-vlm 0.6.15の巻き戻しガードはこの条件を検査しないため、`trim`を持たない要素に対して`c.trim(n)`へ到達していました。巻き戻しの可否をランタイムより手前でMLXBar自身が判定し、不可能な場合は完了ターンのスナップショットを復元します。判定材料はメソッドの存在だけなので、上流が修正すれば自動的に本来の経路へ戻ります。
- **ディスクキャッシュ上限に1件も収まらないとき、書いては即座に捨てる動作になっていた問題を修正しました。** 1トークンあたりのキャッシュ量をモデルの`config.json`から算出し、収まらないと判定した時点で理由付きでディスク層を停止します（`Qwen3.8-27B-MLX-8bit`は64 KB/トークン、10万トークンで6.4 GB）。
- **ランタイムスロットの切り替えが、生成中でも実行できていた問題を修正しました。** 明示的なアンロードと同じく、実行中・待機中の生成があれば拒否します。

### 追加

- **再利用が止まったことを表示します。** メニューバーにロード中モデルの再利用方式を出し、プロンプト全体の再計算が2回続いた時点で理由付きで警告します。
- **`cache_tier: cold`に理由を記録します。** `reuse_unsupported` / `budget_insufficient` / `cancelled_previous` / `memory_pressure` / `token_ids_unavailable` / `write_budget_reached` / `runtime_changed` / `no_prefix` / `first_request`。閉じた語彙で、未知の値はNULLとして保存します。既存DBには`cold_reason`・`shared_prefix_tokens`・`held_prefix_tokens`列を自動migrationします。
- **長いprefillに残り時間の見積りを出します。** Worker自身が完走した要求から測ったprefill速度とトークナイザ比率から算出し、1件も完走していないうちは表示しません。
- **直前要求との共通prefix長を記録します。** system promptへの時刻混入やtool順序の非決定性など、クライアント側で再利用を壊している原因を発見できます。会話本文は保存しません。
- **`GET /v1/models`が`prefix_reuse`と`recommended_max_prompt_tokens`を返します。**
- **再起動後に前回のモデルを自動で再ロードします**（`general.preloadLastModel`、既定有効）。利用者が自分でアンロードした場合は行いません。
- **再利用可否が変化するとランタイム履歴へ記録します。** 上流の修正が効いたことが1行で分かります。
- `scripts/cache-capability-matrix.py`を追加しました。重みをロードせず、`config.json`だけからローカル全モデルの再利用方式と必要容量を一覧します。
- 設定`promptCache.branchCheckpoint`（`auto`/`off`）、`promptCache.diskWriteBudgetGB`（既定32）、`promptCache.memoryBlocks`（`auto`/`off`、**既定off**）を追加しました。ブロックプールは27B級ハイブリッドでの実測がないため既定では有効にしません。

### 検証

- Pythonテスト253件が成功（中断・枝分かれ・容量算出・スナップショット往復を対象とする37件を追加）。
- Swiftリリースビルドが成功。
- 実機計測は`TEST_PLAN_v1.6.0.md`に記載。27 GBの重みを要するものは区別して明示しています。

## [1.5.3] - 2026-08-22

### 修正

- **公開APIが、認証情報のない要求でも本文を最後まで読んで解析していた問題を修正しました。** FastAPIはハンドラ引数の`body`をハンドラ本体の`authorize()`より先に解釈するため、資格情報を持たない相手でも送った分だけメモリを確保させられました。実測では認証なしの40MB要求24並列でCoordinatorのRSSが60MBから1,822MBまで増え、要求完了後も戻りませんでした。認証をASGI層へ移し、同じ条件で60MB→69MBに収まることを確認しています。
- **要求サイズの上限を追加しました。** `Content-Length`が上限を超える要求は本文を読まずにHTTP 413で拒否し、`Transfer-Encoding: chunked`では受信量が上限に達した時点で打ち切ります。上限は固定値ではなく設定から算出します（`maxPromptCharacters`×4 + `maxImages`×`maxImageBytes`×4/3 + 1MB、既定で約281MB）。画像8枚をdata URIで送る要求は正当に280MBへ達しうるため、固定値にすると画像入力が壊れます。`api.maxRequestBytes`に明示値を設定すると上限を下げられます。
- **同時接続数の上限`api.maxConcurrentConnections`（既定64）を追加しました。** ピークメモリが接続数で決まる性質があるため、決定的に頭打ちにします。
- 認証失敗をAPIログへ`AUTHENTICATION_FAILED`として記録します。`/health`は監視用に認証不要のままです。

### 追加

- **生成中のトークン毎秒をメニューバーへ表示します。** 「モデルが応答を生成中」の下に現在の速度を出します。ZCodeやLAN内の別PCから届いた要求でも表示されます。
- 値はランタイム自身が各トークンで計算している`generation_tps`（prefillを除外した最初のトークンからの平均）を使い、ランタイムが提供しない場合はWorker側の実測へ自動的に切り替わります。将来のランタイム更新で項目が失われても表示が消えるだけで、生成には影響しません。
- **テキストを出さないトークンでも計測します。** 実機の`Qwen3.5-9B-MLX-8bit`は111トークン分を14秒間ため込んでから1つの`delta`で吐くため、`delta`を数える方式では何も表示できませんでした。生成ループ側の刻みで計測します。
- ランタイムの最初のサンプルは分母がほぼ0で数万トークン毎秒という無意味な値になるため、3トークンに達するまで公開しません。
- 進捗イベントはOpenAI応答の本文には混入せず、SSEのkeep-aliveとして扱います。診断は`GET /api/v1/status`の`generationTokensPerSecond`と`generatedTokens`で確認できます。

### 検証

- 認証なし40MB×24並列でRSSが60→69MB（v1.5.2は60→1,822MB）に収まることを実測しました。認証なしのPOSTは401、`/health`は200、上限超過は本文を転送せず413、正当な最大サイズ279MBは事前拒否されないことを確認しています。
- チャンク送信で419MBを流し、上限10MB設定ではRSS増加が33MBに留まることを確認しました。増加分はアプリ側であり、上限がそのまま効きます。
- 実機`Qwen3.5-9B-MLX-8bit`で、`delta`が1つしか出ない生成でも進捗が毎秒発火し、`status`へトークン毎秒が反映され、生成終了後はフィールドごと消えることを確認しました。OpenAI応答本文への混入は0件です。
- Pythonテスト212件（v1.5.2比+16件）が成功しました。Swift Debug／Release buildも成功しています。

## [1.5.2] - 2026-08-22

### 修正

- **v1.5.1のプロンプトキャッシュ再試行判定が広すぎ、無関係なモデルエラーまで再試行していた問題を修正しました。** mlx-vlmの`stream_generate`は巻き戻し処理と同じ`generate/dispatch.py`で定義されているため、「tracebackにそのモジュールがあるか」で判定すると、生成中のあらゆるエラーが該当してしまいます。結果として、通常のモデルエラーでもprefillを1回無駄にし、さらに再試行前の`_reset_prompt_cache()`で暖まったキャッシュを捨てていました。
- 判定をモジュールではなく**失敗した呼び出し自体**へ変更しました。tracebackの各フレームについて失敗行のソースを読み、`.trim(`を含む場合だけ巻き戻しの失敗と判断します。行番号ではなくソース内容を見るため、ランタイムのバージョン差に影響されません。ソースが読めない場合に備え、`trim`を含むAttributeErrorも補助的に許容します。

### 検証

- 実runtimeで両方の失敗を分類し、巻き戻し失敗は`True`、通常のモデルエラー（`'NoneType' object has no attribute 'config'`）は`False`になることを確認しました。v1.5.1ではどちらも`True`でした。
- 同一モジュール内で「trimを呼ぶ関数」と「呼ばない関数」を用意して分類する回帰テストを追加しました。このテストはv1.5.1の判定では失敗します。
- **上流が修正された場合の前方互換を確認しました。** mlx-vlmの保持判定に`trim`の有無チェックを足した状態を再現し、継続ターンも枝分かれターンも`cache_tier=disk`で成功し、`reuseFailures`が0のままであることを実機で確認しています。回避策は例外発生時にしか動かないため、上流修正後は自然に発火しなくなります。
- Pythonテスト196件が成功しました。

## [1.5.1] - 2026-08-22

### 修正

- **Qwen3.5／3.8系のモデルで、会話が枝分かれしたターンが`GENERATION_FAILED`（`'ArraysCache' object has no attribute 'trim'`）で失敗する問題を回避しました。** mlx-vlmはキャッシュを共通prefixまで巻き戻す際、各エントリの`trim()`を呼びますが、その事前確認が`is_trimmable()`ではなく別の保持状態チェックのため、`trim`メソッド自体を持たないhybrid層（`ArraysCache`）で例外になります。プロンプト再利用は最適化に過ぎないため、応答をまだ送信していなければ新しいキャッシュで一度だけ再試行し、生成そのものは成功させます。
- 巻き戻しの失敗はprefill前に即座に起きるため、再試行の追加コストは実質ありません。ディスクキャッシュは再試行でも参照されるため、該当ターンはcold prefillではなくdisk hitになります。
- 回復回数を`GET /api/v1/prompt-cache`の`reuseFailures`へ記録します。ランタイム側の失敗と本物のモデルエラーはtracebackで区別し、後者は従来どおりそのまま報告します。

### 検証

- 実機`Qwen3.5-9B-MLX-8bit`（同じqwen3_5 hybrid構成）で、システムprefixだけを共有する枝分かれ要求を送って失敗を再現し、修正後は同じ手順が成功して`cache_tier=disk`（1,174 tokens再利用）になることを確認しました。
- **この不具合はv1.5.0の変更が原因ではありません。** v1.4.1のコードでも同じ手順で再現することを確認済みです。プロンプト再利用をmlx-vlmへ渡す実装はv1.3.7から変わっていません。
- ランタイムの再利用機構の失敗だけが再試行対象で、通常のモデルエラーは再試行されないことを回帰テストで固定しました。Pythonテスト195件が成功しています。

## [1.5.0] - 2026-08-22

大規模モデルの常用（ZCode + Qwen 27Bクラスの長時間運用）を想定した設計精査の結果をまとめて反映したリリースです。メモリ安全性、クラッシュ安全性、効率、OpenAI互換性の各面を強化しました。

### 追加

- **mlx-lmモデルにも二層プロンプトキャッシュを追加しました。** これまでプロンプト再利用はmlx-vlm経路にしかなく、テキスト専用モデルはZCodeが毎ターン再送するsystem prompt・tools定義・履歴を毎回先頭から計算していました。mlx-lm公式の`LRUPromptCache`をRAM層、`save_prompt_cache` / `load_prompt_cache`によるprefix snapshotをディスク層として組み込みます。OpenAI互換のmessages・tools・tool callingの意味は変更しません。
- **MLXのwired limitとcache limitをモデルロード時に明示するようにしました。** 上限を設定しないと、大きなモデルの重みがmacOSにページアウトされて生成速度が桁で落ちます。`generation.wiredLimitRatio`（既定0.80）と`generation.cacheLimitRatio`（既定0.10）で調整でき、0で従来どおりランタイム既定に任せます。
- **生成中もメモリを監視して停止できるようにしました。** 従来の安全確認は生成開始前の1回だけで、長い入力と長い応答でKVキャッシュが伸びる区間を見ていませんでした。生成中は5秒間隔で確認し、上限に達した要求だけを`MEMORY_PRESSURE`で停止します。プロセスがOSに強制終了されるより、要求1件の失敗の方が安全です。
- **`generation.tokenIdleTimeoutSeconds`を実装しました。** 設定項目としては存在しながら未実装でした。Workerはprefill中もheartbeatを送るため、この時間の完全な無音はWorkerの停止を意味します。該当時のみプロセスを再起動します。
- **OpenAIの`stop`シーケンスと`seed`に対応しました。** どちらも受理しながらMLXランタイムへは渡していませんでした。`stop`は分割配信されたトークンをまたいで検出します。
- **プロンプトキャッシュの世代を自動回収するようにしました。** namespaceはモデルと ランタイム版から作られるため、モデル切替やランタイム更新のたびに新しい世代が増え、容量上限は1世代内にしか効いていませんでした。`promptCache.keepGenerations`（既定2）を超えた古い世代を削除します。

### 修正

- **クライアントが切断してもWorkerの生成器が閉じられず、中断時のプロンプトキャッシュ破棄が働かない問題を修正しました。** ZCodeがストリームを打ち切ると`lines()`は破棄されますが、adapterの生成器へ終了が伝わらず、「途中まで進んだキャッシュとtoken_idsの対応が壊れるのを防ぐ」ための破棄処理が実行されていませんでした。結果として、次の要求が整合しないキャッシュを再利用しうる状態でした。終了時に必ずMLXスレッド上で生成器を閉じ、Coordinatorからも`cancel`を通知します。
- **待機中にモデルが降ろされると、待機列の要求がすべて`AttributeError`で失敗する問題を修正しました。** キュー復帰後にモデルの再確認をしておらず、しかもその行が生成枠を解放するブロックの外にあったため、ロックが一時的に不整合になっていました。現在はHTTP 409 `MODEL_NOT_LOADED`を返し、生成枠をその場で解放します。
- **モデルのロード・アンロードが待機中の生成を確認しない問題を修正しました。** OpenAI経路の自動ロードは実行中要求だけを見ており、管理APIの`POST /models/{id}/load`と`DELETE /models/loaded`はどちらも見ていませんでした。実行中・待機中のいずれかがあれば`ENGINE_BUSY`を返し、GUIからの明示的な強制実行だけが迂回できます。
- **`finish_reason`が常に`stop`で、応答の切り詰めをクライアントへ伝えられない問題を修正しました。** `max_tokens`に達した応答も正常完了として届くため、ZCodeは継続が必要な場面を判定できませんでした。ランタイムの判定と生成token数から`length`を返します。
- **`<tool_call>`以外のtool記法が本文としてもtool callとしても二重に配信される問題を修正しました。** ランタイムのtool parserは`<|tool_call_start|>`、`<minimax:tool_call>`、`<atem:function_calls>`など複数の記法を解釈しますが、ストリーム側は`<tool_call>`しか検出していませんでした。検出側の記法をparser側に揃えました。
- **ディスクキャッシュ障害の判定が緩く、無関係なモデルエラーでキャッシュを無効化しうる問題を修正しました。** 例外メッセージの`safetensors`だけで一致していましたが、これは通常の重み読み込みエラーにも現れます。tracebackによる判定を主とし、曖昧でないマーカーだけを補助に使います。
- **完了したジョブのイベントキューが解放されない問題を修正しました。** 常時起動のCoordinatorでは、スキャンとランタイム更新の回数だけ単調に蓄積していました。

### 変更

- **ランタイムの自動更新チェックを既定で無効にしました。** 動作中の大規模モデルの下でランタイムが変わると、永続プロンプトキャッシュのnamespaceが無効化され、それまで動いていた構成が壊れることがあります。更新は明示的な操作のみとします。
- **生成の安全上限（`totalTimeoutSeconds`）を900秒から3600秒へ延ばし、超過時もモデルをロードしたまま維持するようにしました。** 27Bクラスでは長い1ターンが900秒を超えることがあり、超過時の処理がWorkerの強制終了とモデルのアンロードだったため、以後の要求が数分の再ロードから始まっていました。単に遅い生成と、応答しないWorkerを別の経路として扱います。
- **`response_format`と`logprobs`を黙って無視せず、HTTP 400で明示的に拒否するようにしました。** 未知のベンダー拡張を無視するのは互換性のために正しい一方、クライアントが依存している標準項目を無視すると、原因の分からない挙動として表面化します。`response_format: {"type": "text"}`は従来どおり動作します。
- **プロンプトキャッシュのディスク上限の既定値を5 GBから10 GBへ引き上げ、RAM層にも上限を設けました。** 8,000 token規模のsnapshotは1 GBに達するため、5 GBでは実用的な世代数を保持できません。RAM層は`promptCache.memoryRatio`（既定0.10、物理メモリ比）で上限を設けます。
- **メモリ安全判定を物理メモリ総量比だけでなく、空きメモリとmacOS自身のメモリ逼迫レベル、プロセスの実常駐サイズでも行うようにしました。** 総量比は他のアプリの使用量を見られないため、MLX単体では上限未満でもマシン全体はスワップしている状態を検出できませんでした。
- **設定ファイル破損時の初期化を状態へ記録するようにしました。** バックアップは従来どおり作成しますが、通知経路がなく、LAN公開やポートが静かに既定へ戻っていました。
- **APIログの剪定を毎回ではなく一定間隔で行い、APIキーをmtime付きでキャッシュするようにしました。** どちらも全要求がイベントループ上で同期I/Oを踏んでいました。

### 検証

- Pythonの単体・契約・結合・障害注入・ネットワーク・並行stressテスト193件（v1.4.1比+22件）が成功しました。
- 実uvicorn + Unixドメインソケットで生成中に切断し、adapterの生成器がMLXスレッド上で閉じられること、以後トークンが生成されないことを確認しました。未修正版では生成器が閉じられないことも併せて確認しています。
- 待機中unloadの再現テストで、HTTP 409が返り生成枠が即座に解放され、孤立ロック回復に頼らないことを確認しました。
- 実機`llm-jp-4-32b-a3b-thinking-4bit`（mlx-lm、16.8 GB）へ23 tools・8,024 tokenの合成ZCode入力を送り、cold 5.563秒に対しRAM hit 0.051秒（8,023 tokens再利用）、最初の質問を変更しても0.116秒（8,013 tokens再利用）、Worker再起動後のディスクhitは0.673秒（7,768 tokens再利用）でした。
- 古いキャッシュ世代3件から`keepGenerations=2`で現行世代と直近1件だけが残ることを確認しました。
- `ru_maxrss`が高水位のまま下がらないことを実測し、メモリ判定が現在の常駐サイズを使うことを回帰テストで固定しました。
- Xcode 26.6でSwift Debug／Release buildが成功しました。

## [1.4.1] - 2026-08-21

### 修正

- **ZCodeのストリームがprefill中または生成キューから実行へ移る途中で切断されると、実行中要求が0件でも生成ロックだけが残り、以後の要求が永久に待機する問題を修正しました。** OpenAI APIとクイックチャットのSSE終了時に内側の生成器を明示的に閉じ、生成枠へ要求ID付きの所有権を追加しました。
- GUI状態更新、新規要求、キューheartbeat、idle待機時に、所有者が実行中にも待機中にも存在しない孤立ロックだけを検出して自動解放します。正常なqueue→active移行は所有者が登録され続けるため回復対象にならず、古い要求の終了処理が新しい要求のロックを解放することもありません。
- 診断状態へ`generationLockState`（`idle` / `active` / `handoff` / `inconsistent`）と、起動後の自動回復回数`generationLockRecoveries`を追加しました。要求IDや本文は公開しません。

### 検証

- 実機で確認した`activeRequestCount=0`、`queuedRequestCount=1`、Worker CPU idleの孤立状態を障害注入で再現し、状態取得だけでロックが回復して次要求が完了することを確認しました。
- 正常なqueue handoffの誤回復防止、別要求によるrelease拒否、待機済み要求の起床、SSEとアクセス記録wrapperの明示終了、100並列要求の約3分の1を途中切断するstressを追加し、Pythonテスト171件、Swift Debug／Release buildが成功しました。

## [1.4.0] - 2026-08-21

### 改善

- **mlx-vlmモデルで、Worker再起動後の最初のZCode会話にも長い共通prefixを再利用できる永続cacheを追加しました。** mlx-vlm公式`APCManager` / `DiskBlockStore`をディスク専用層として組み込み、v1.3.7の`PromptCacheState`を高速なRAM層として維持します。OpenAI互換messages・tools・tool callingの意味は変更しません。
- Qwen3.8等のhybrid exact-cacheでは末尾256 tokensをcoldに残す境界snapshotを保存し、最初のユーザー文が変わっても、その前の大きなsystem prompt・tools定義を再利用できるようにしました。
- モデルパス、設定/tokenizer/chat template、重みのsize/mtime、mlx-vlm版、cache形式版からnamespaceを分離します。APCは安定SHA-256 block hashを使用し、最大5 GB（設定で1〜100 GB）のLRU disk tierとして動作します。
- APC障害が応答開始前に発生した場合だけ、既存のPromptCacheState/cold経路で一度再試行します。APCと無関係なモデルエラーは再試行せず、そのまま報告します。
- 設定画面へ「キャッシュ」を追加し、有効/無効、容量上限、使用量、disk hit、RAM/disk個別消去を操作できるようにしました。APIログには本文を保存せず`cold` / `memory` / `disk`のcache tierを記録します。
- FastAPI／Starletteのテストクライアントを新しい`httpx2`経路へ移行し、旧`httpx`互換経路の非推奨警告を解消しました。本体のHTTP通信は従来どおり安定版`httpx`を使用します。

### 検証

- Disk APCのlayering、モデル/runtime指紋、容量上限、ディレクトリ権限、破損時fallback、無関係なエラーの非再試行、管理API、設定検証、DB移行を回帰テストへ追加しました。
- Pythonテスト164件（非推奨警告0件）、Swift Debug／Release build、署名、DMG構造検証が成功しました。実機`Qwen3.8-27B-MLX-8bit`へ15,852-tokenの合成ZCode入力を送り、cold 56.106秒に対してWorker再作成相当のdisk hitは15,596 tokensを再利用して1.531秒、最初のユーザー文を変更しても同じ15,596 tokensを再利用して1.414秒、次のRAM hitは15,862 tokensを再利用して0.262秒でした。APC RAM blocksは0、reject/fallbackは0でした。

## [1.3.7] - 2026-08-21

### 改善

- **ZCodeからQwen 27Bへ短い挨拶を送るだけでも、最初の応答まで約30秒以上かかる問題を改善しました。** ZCodeは毎ターン多数のtools定義・system指示・履歴を送るため、v1.3.6では同一prefixも27Bモデルが毎回cold prefillしていました。mlx-vlm公式の`PromptCacheState`をテキスト要求へ組み込み、直前要求との最長共通token prefixを再利用します。messages・toolsを省略または改変しないため、OpenAI APIとZCodeのtool calling semanticsを維持します。
- 画像要求では異なる画像が同じplaceholder tokenを持ち得るためprompt cacheを共有しません。キャンセル・生成失敗時は途中キャッシュを破棄し、メモリ安全上限到達時はキャッシュだけを解放して再判定します。
- APIアクセスログへ本文を保存しない性能情報（入力/tool定義の文字数、first-token時間、prompt/cached token数、prefill・生成速度、推論モード）を追加しました。既存データベースは起動時に自動移行します。
- mlx-lm・mlx-vlmが返す実token使用量と生成メトリクスをCoordinatorへ伝播し、ストリームのusage精度を改善しました。
- OpenAI系クライアントの`reasoning_effort=high` / `minimal`をまずそのまま試し、Qwenテンプレートが拒否した場合だけ`xhigh` / `low`へ再試行します。

### 検証

- 実機の`Qwen3.8-27B-MLX-8bit`と23 tools（55,881文字、10,677 prompt tokens）で、cold時のfirst token 35.253秒に対し、次ターンは10,688 tokensを再利用して0.399秒（約88倍高速）を確認しました。
- prefix cache、画像分離、キャンセル時破棄、メモリ圧迫時解放、DB移行、性能ログ、usage/metrics、推論強度aliasを回帰テストへ追加し、Pythonテスト156件とSwiftリリースビルドを検証しました。

## [1.3.6] - 2026-08-21

### 修正

- **クイックチャットは動く一方、ZCodeでは長時間応答が表示されない問題を修正しました。** ZCodeがtool calling用のツールを送ると、従来のWorkerはツール判定のため全生成テキストを完了まで保持していました。通常本文を即時配信し、分割された`<tool_call>`開始だけを検出して以降を解析用に保持する増分処理へ変更しました。
- QwenのthinkingをOpenAI互換ストリームの`reasoning_content`へ分離し、`<think>`・`</think>`・`<assistant>`・`<tool_call>`を通常本文へ漏らさないようにしました。tool call解析失敗時は`TOOL_PARSE_FAILED`を返します。
- 同梱Workerの実行時にPythonがアプリバンドル内へ`__pycache__`を作成し、起動後のコード署名検証を壊す問題を修正しました。Coordinatorランチャー、fallback LaunchAgent、Worker環境でbytecode生成を無効化しました。

### 検証

- 通常本文の増分配信、thinking分離、分割tool marker、tool call確定、解析失敗、OpenAI SSE変換を回帰テストへ追加し、Pythonテスト148件とSwiftリリースビルドが成功しました。

## [1.3.5] - 2026-08-21

### 修正

- **v1.3.4でもZCodeからHTTP 400 `UNSUPPORTED_PARAMETER`が返る問題を修正しました。** OpenAI互換クライアント固有の未知の拡張項目をホワイトリストで拒否せず、安全に無視する方式へ変更しました。トップレベルだけでなく`extra_body`内の`thinking`・`reasoning_effort`と`thinking.effort`もQwen系チャットテンプレート向けに正規化します。
- 入力拒否時は会話本文・応答本文・APIキーを保存せず、モデル名とエラーコードをAPI監査ログへ、拒否項目名をCoordinatorログへ記録するようにしました。

### 検証

- 未知のトップレベル項目、`extra_body`内の追加項目、複数のthinking形式、明示値の優先順位、安全上予約されたテンプレート引数、監査ログを回帰テストに追加し、Pythonテスト144件が成功しました。

## [1.3.4] - 2026-08-21

### 修正

- **ZCodeがQwen3.8等の推論モデルへ送るトップレベルの`thinking`をHTTP 400 `UNSUPPORTED_PARAMETER`で拒否する問題を修正しました。** `thinking.type`を`enable_thinking`、`budget_tokens`を`thinking_budget`、`clear_thinking`を逆値の`preserve_thinking`へ正規化し、mlx-lm・mlx-vlm両方のチャットテンプレートへ伝播するようにしました。受理するだけで実質無視していたトップレベルの`reasoning_effort`も同名のテンプレート引数へ渡します。既存の`extra_body.chat_template_kwargs`による明示値を優先し、不正な形はモデルロード前に入力エラーで終了します。

### 検証

- ZCode形式の`thinking`、推論強度、budget、履歴保持、明示値の優先順位、不正入力を回帰テストに追加し、Pythonテスト142件が成功しました。

## [1.3.3] - 2026-08-20

### 修正

- **ZCodeがOpenAI互換APIへ送る`extra_body.chat_template_kwargs`をHTTP 400 `UNSUPPORTED_PARAMETER`で拒否する問題を修正しました。** `extra_body`の形をCoordinatorで検証し、`enable_thinking`や`reasoning_effort`などのモデル固有値をmlx-lm・mlx-vlm両方のチャットテンプレートまで伝播するようにしました。tool callingの段階的フォールバック時にもこれらの値を保持します。MLXBar自身が管理する`tools`、`tool_choice`、`tokenize`等の上書きは入口で拒否します。

### 検証

- ZCode形式の正常系、不正入力、mlx-lm・mlx-vlmへの伝播、tool calling併用を回帰テストに追加し、Pythonテスト138件とSwiftのリリースビルドが成功しました。

## [1.3.2] - 2026-08-20

外部クライアント(ZCode)からmlx-vlmモデルへ接続した際に生成が失敗する不具合を修正するパッチリリースです。

### 修正

- **mlx-vlmモデルでの生成が`apply_chat_template requires jinja2 to be installed.`で失敗する問題を修正しました。** チャットテンプレートの適用(`apply_chat_template`)は最終的にHugging Face `transformers`のtokenizer/processorが担当しますが、`transformers`自体は`jinja2`を必須依存にしていません。ランタイムのインストール処理([Coordinator/mlxbar/runtimes/updater.py](Coordinator/mlxbar/runtimes/updater.py))は`uv pip install`で`mlx-lm`/`mlx-vlm`本体・`fastapi`・`uvicorn`のみを明示指定しており、`jinja2`を指定していませんでした。実機の`requirements.lock`を比較したところ、`mlx-lm`側はたまたま別の依存経由で`jinja2`が入っていた一方、`mlx-vlm`側には全く含まれておらず、画像対応モデルでチャットテンプレートを適用するたびに`ImportError`で生成が失敗していました。インストールコマンドに`jinja2>=3.1,<4`を明示追加し、両エンジンで確実にインストールされるようにしました。詳細な調査記録は[mlx-bar.md](mlx-bar.md)を参照してください。

### 備考

- Swift側(GUI)は今回変更していません。既存のCoordinatorテスト134件は無変更で成功しています。
- 影響を受けるのは、この不具合が入った状態で`mlx-vlm`ランタイムを新規インストール・更新した環境のみです。既にインストール済みのランタイムスロットは、ランタイム更新画面から更新するか、`mlxbarctl runtime`で再インストールしてください。

## [1.3.1] - 2026-08-20

v1.3.0のリリース直後、実機で「メニューバーのアイコンをクリックすると、ポップアップが開く前に一瞬で消える」不具合が発覚し、その修正のみを目的としたパッチリリースです。

### 修正

- **メニューバーのアイコンをクリックするとアプリが即座にクラッシュする問題を修正しました。** [Services/Localization.swift](Sources/MLXBar/Services/Localization.swift)の`AppLanguage.bundle(for:)`が、SwiftPM自動生成の`Bundle.module`アクセサ経由でローカライズ資材(`MLXBar_MLXBar.bundle`)を読んでいましたが、このアクセサ(executable target向け生成コード)は`.app`直下(`Bundle.main.bundleURL`)を探す実装でした。一方`scripts/build-release.sh`は同バンドルを`Contents/Resources/`配下に配置しており([Services/CoordinatorClient.swift](Sources/MLXBar/Services/CoordinatorClient.swift)の`bundleResources`もこちらの規約に従います)、探索先と実配置がずれていたため、メニューを開いてローカライズ文字列を読もうとするたびに`resource_bundle_accessor.swift`生成コードの`fatalError`でプロセスごと終了していました。GUI言語設定が日本語(ソース言語)のときはこのコードパスを通らないガードがあるため症状が出ず、開発環境では踏みにくい一方、新規インストールでは`guiLanguage`が既定で`"en"`のため高確率で再現する状態でした。`AppLanguage`側の資材解決を`CoordinatorClient`と同じ`Contents/Resources`パスに統一し、`.app`化されていない`swift run`実行時のみ`Bundle.module`にフォールバックするようにしました。詳細な調査記録は[mlx-bar.md](mlx-bar.md)を参照してください。

## [1.3.0] - 2026-08-20

v1.2.4のリリース検証中に実機で発生した「インストール直後にCoordinatorがクラッシュする」不具合の調査を発端に、CLI(`mlxbarctl`)のGUIとの機能パリティ、起動信頼性、「すべてのデータを削除」の完全性をまとめて強化しました。

### 追加

- **`mlxbarctl`から、これまでGUI専用だった操作の大半を実行できるようになりました。** 生成の一括キャンセル(`cancel-all`)、ランタイムのslot削除・ジョブキャンセル(`runtime delete-slot`/`runtime cancel-job`)、APIキー・LM Studioキーの参照・保存・再生成(`secrets ...`)、APIアクセスログの表示・消去(`logs show`/`logs clear`)、LAN公開のアトミックな切り替え(`network set-lan --enabled/--disabled`)、ポート変更(`network set-port`)、言語・Max token上限・並列リクエスト上限・既定の生成パラメータ・モデルフォルダの追加削除(`config set-*`/`model add-folder`/`model remove-folder`)、「すべてのデータを削除」(`remove-all-data --yes`)を追加しました。「ログイン時に起動」はCoordinator側の設定値のみCLIから変更可能です(`SMAppService`がSwift専用APIのため、実際のOS登録はGUI起動時に反映されます。詳細は`--help`を参照)。

### 修正

- **インストール直後にバックグラウンドサービス(Coordinator)がクラッシュし、サービスが起動しないことがある問題を軽減しました。** 原因は、ad-hoc署名（ビルドごとに署名IDが変わる）のもとで、過去にインストールされた別ビルドのlaunchd登録が同じラベル(`com.yukiorita.MLXBar.Coordinator`)に残っていると、新しいインストールの起動時に競合し、OSにサービスをSIGKILLされる（Launch Constraint Violation）ことがある点でした。起動シーケンスに、登録が競合している可能性がある場合の防御的な再登録ステップ（両登録経路のbootout→再登録→再試行）を追加し、`.requiresApproval`状態も初回起動でその場で検知するようにしました。
- **Coordinatorがクラッシュしても記録が一切残らないことがある問題を修正しました。** launchdの設定ファイルはセキュリティ上の理由でstderrを`/dev/null`に送るため、`logging`モジュールの初期化前に起きた例外や、どこにも捕捉されない例外は完全に失われていました。`logging`の状態に依存しない緊急ログ(`~/Library/Application Support/MLXBar/logs/coordinator-crash.log`)を追加しました。
- **「すべてのデータを削除して終了」を実行しても、次回インストール時に問題が引き継がれることがある構造を見直しました。** データの実体（設定・トークン・データベース・ランタイム・uvキャッシュ・workerソケット）の削除を、それぞれの実体を最もよく知るCoordinator自身が担う新しいエンドポイント(`POST /api/v1/system/reset`)に一本化しました。GUIの「すべてのデータを削除して終了」と`mlxbarctl remove-all-data`の両方がこのエンドポイントを呼びます。macOSのログイン項目登録(`SMAppService`)の解除だけはSwift専用APIのため`mlxbarctl`からは行えませんが、`launchctl bootout`でサービス自体は確実に停止するため、次回インストールの動作には影響しません（システム設定のログイン項目に見た目上のエントリが残るのみです）。
- 上記の調査中に、Coordinator起動シーケンスに存在した別の競合（管理APIがリクエストを受け付け可能になるタイミングが、起動時の自動ランタイムインストール処理より早く、両者が競合するとクラッシュしうる）も見つけて修正しました。
- **さらに調査を進めたところ、Coordinatorが`/api/v1/health`で返すバージョン文字列がv1.1.0のまま、v1.2.0〜v1.2.4の3リリースにわたって更新されていなかったことが判明しました。** アプリの起動処理はこの値とアプリ自体のバージョンが完全一致することを健全性の条件にしているため、常に不一致となり、Coordinatorが正常に動作している場合でも**アプリを起動するたびにバックグラウンドサービスの登録を毎回作り直していた**可能性があります（登録をやり直す回数が増えるほど、上記のLaunch Constraint Violationに遭遇する機会も増えていたと考えられます）。バージョン文字列を修正し、以後のリリースチェックリストにも追加しました。

### 備考

- 詳細な設計判断（`POST /api/v1/system/reset`のシャットダウン順序、CLIからはmacOSのSMAppServiceを直接操作できない制約など）は`mlx-bar.md`に記録しています。

## [1.2.4] - 2026-08-20

利用者からの「メニューバーのアイコンをクリックすると消える」という報告を受けて調査し、根本原因を修正しました。あわせて、その調査の過程でUIの安定性・メモリ安全性・クラッシュ安全性を精査し、見つかった課題をまとめて修正しています。

### 修正

- **メニューバーのアイコンをクリックすると、フラッシュして一瞬見えなくなる問題を修正しました。** メニューを開いている間、`MenuBarView`は1秒間隔で`refreshStatus()`を呼びますが、値が変化していなくても`@Published`プロパティを無条件に再代入していたため、状態が同じでも毎秒SwiftUIへ「変更あり」が伝わり、ステータスバーの`Label`とポップアップ本体が再描画され続けていました。`MenuBarExtra`の`.window`スタイルは内容が頻繁に変化すると理想サイズの計算が崩れやすいことがv1.2.1／v1.2.2で既に確認されており（[mlx-bar.md](mlx-bar.md)）、今回の消失も同系統の不安定さでした。値が実際に変化したときだけ書き込む`setIfChanged`ヘルパーでガードしました。
- **サービス停止中もメニューバーアイコンが起動中と同じ`cpu`のままだった問題を修正しました。** アイコンを決める分岐の最後が`serviceRunning ? "cpu" : "cpu"`という左右同一の三項演算子になっており、停止状態を示すアイコンが存在していませんでした。停止中は`moon.zzz`を表示するようにしました。
- **チャット生成のストリーミング用curlプロセスの起動に失敗すると、Pipeのハンドラが解放されずリークしうる問題を修正しました。** `CoordinatorClient.stream()`は`process.run()`が失敗した場合、ハンドラ解除コードに到達する前に例外を投げていたため、`Pipe`とそれが握る`MenuBarViewModel`ごと解放されない経路がありました。`defer`でハンドラ解除を保証しました。
- **`launchctl bootout`や`defaults delete`が応答しない場合、「すべてのデータを削除して終了」がスピナー表示のまま止まらなくなる可能性を修正しました。** これらの呼び出しには、`request()`のcurl呼び出しが持つ`--max-time`に相当するタイムアウトがありませんでした。`runProcess`にウォッチドッグ方式のタイムアウトを追加。プロセスの正常終了とタイムアウト発火が競合しても二重resumeで落ちないよう、`SingleResume`で保護しています。
- **ランタイム管理画面のスロット・更新履歴一覧が、配列のインデックスをSwiftUIの`ForEach` IDとして使っていた点を修正しました。** 一覧の並びや件数が変わると行のアニメーション・状態がずれる可能性があったため、バックエンドが返す`id`（スロット）や`created_at`／`action`／`slot_id`の組み合わせ（更新履歴）を安定したIDとして使うようにしました。
- **複数の`Task`クロージャが`self`を無条件に強参照していた箇所を`[weak self]`に変更しました。** `attachRuntimeJob`・`copyLoadedModelName`・チャット生成のストリーミングコールバックが対象です。いずれも実害は小さいものの、防御的に修正しています。

### 備考

- Coordinator（Python側）は今回変更していません。既存のテスト125件は無変更で成功しています。

## [1.2.3] - 2026-08-19

利用者からの不具合報告3件を修正しました。

### 修正

- **設定画面「モデル」タブの生成パラメータ（温度・Top P・繰り返しペナルティ）のスライダーが幅の制限なく伸び、ウィンドウからはみ出していた問題を修正しました。** `LabeledContent`が全行で共有する配置カラムの計算が崩れ、スライダー行が際限なく広がると同時に、長いラベル（特に英語UI）が左端で欠けたり、説明文が折り返さずウィンドウ端で切れたりしていました。パラメータ行をラベル・入力欄・スライダーが縦に並ぶ独立したブロックへ再構成し、幅を明示的に制限。説明文にも折り返し用の`.frame(maxWidth: .infinity, alignment: .leading)`を追加しました。なお`.fixedSize(horizontal:vertical:)`はv1.2.2で確認された「サイドバーが空白表示になる」不具合を誘発するため使用していません（詳細は[mlx-bar.md](mlx-bar.md)）。合わせてFormがウィンドウ内でスクロールしていなかった問題も、明示的な`ScrollView`で解消しました
- **モデルによっては、ツール呼び出し（tool calling）付きのリクエストで応答が返らなくなる問題を修正しました。** v1.2.1で、チャットテンプレート適用時の例外処理を「`tool_choice`非対応による`TypeError`」だけに絞ったところ、`tools`自体に対応していないテンプレートがJinja2側で送出する例外（`TypeError`ではない）が未処理のまま生成全体を失敗させるようになっていました。`tools`＋`tool_choice` → `tools`のみ → プレーンなテンプレート、の順に段階的にフォールバックする処理を`Workers/common/tool_calls.py`に共通化し、tools関連以外の失敗は従来通りそのまま表面化させます
- **「すべてのデータを削除して終了」を実行しても、mlx-lm・mlx-vlmのインストールに使ったパッケージキャッシュがMacに残ることがある問題を修正しました。** ランタイムのインストールに使う`uv`のキャッシュがユーザー共有の`~/.cache/uv`に書き込まれており、削除対象のMLXBar専用フォルダの外にあったため、対象になっていませんでした。MLXBar専有の`Application Support/MLXBar/uv-cache`を使うよう変更し、削除対象に含まれるようにしました。また、この修正の過程で、Coordinatorの終了処理がインストール中のジョブを待たずに終了する場合があり、`uv`の子プロセスが孤児化しうる不具合も見つかったため、終了時に実行中のジョブを確実にキャンセルするよう修正しました

## [1.2.2] - 2026-08-19

v1.2.1で発生していた「メニューバーのポップアップが表示されない」不具合を修正しました。

### 修正

- **メニューバーのポップアップがほぼ不可視のサイズ（幅420×高さ10px程度）に潰れ、実質何も表示されなくなっていた問題を修正しました。** 原因はv1.2.1で追加した`ScrollView`ラッパーでした。`MenuBarExtra`の`.window`スタイルは中身のビューが自己申告する理想サイズでポップアップの大きさを決めますが、`ScrollView`は渡された高さいっぱいに広がろうとする性質があり、サイズ計算が破綻していました。v1.0.0／v1.1.0で実績のある、ScrollViewを使わない構成に戻し、実機での複数回の検証でポップアップが正しいサイズで安定して表示されることを確認しました

### 備考

- 設定画面（特にAPIサーバーページ）の余白が広すぎる点も合わせて調査しましたが、修正を試みる過程でSwiftUIの`NavigationSplitView`と`.fixedSize(horizontal:vertical:)`の組み合わせにより、無関係のサイドバーが空白表示になる別の不具合を確認したため、今回は見送りました。設定画面は安定していたv1.2.1と同じ表示のままです

## [1.2.1] - 2026-08-19

v1.2.0で見つかった不具合を追加で修正しました。

### 修正

- **ZCode（OpenAI互換API経由）からLaguna S 2.1を利用すると`GENERATION_FAILED`になる問題を修正しました。** `Workers/mlx_vlm_worker/adapter.py`のチャットテンプレート適用処理が、失敗時に例外を無条件で握りつぶし、未加工のメッセージ配列をそのまま`stream_generate`へ渡していました。トークナイザーが文字列でもリストでもない入力を拒否し、`text input must be of type str...`という分かりにくいエラーになっていました。`tool_choice`非対応が原因の`TypeError`だけを狭く拾って再試行し、それ以外の失敗は本来のエラー内容のまま返すようにしました
- **クイックチャットの入力欄に文字が入力できない問題と、設定画面が見切れる・もっさりする問題を修正しました。** MLXBarはDockアイコンを持たないメニューバー常駐アプリ（LSUIElement）のため、メニューバーのポップアップから別ウィンドウを開いてもアプリがアクティブ化されず、キーボード入力が別のアプリへ流れたり、キーウィンドウでない状態で描画が中途半端になったりしていました。ウィンドウを開く箇所と各ウィンドウの表示時に明示的にアプリをアクティブ化するようにしました

## [1.2.0] - 2026-08-19

利用者からの不具合報告2件を修正しました。

### 修正

- **`<think>`推論ブロックがそのまま応答に混ざる問題を修正しました。** Laguna S 2.1のような推論（thinking）対応モデルがツール呼び出しを伴わない通常のチャット応答を返すと、`<think>...</think>`の内容がそのまま画面に表示されていました。ツール呼び出しマークアップの解析結果が空のときにテキストをそのまま返していたことに加え、タグ除去の正規表現自体もタグだけを消して中身の推論文を消せていなかったため、二重に混入していました。mlx-lm・mlx-vlm両ワーカーに共通のコード（`Workers/common/tool_calls.py`）を修正し、ツール呼び出しの有無にかかわらず`<think>`ブロックを内容ごと除去するようにしました
- **メニューバーのポップアップが画面に収まらないときにスクロールできない問題を修正しました。** 内容が画面の高さを超えると、スクロール手段がないままはみ出した部分が見えなくなっていました。ポップアップの中身をスクロール可能にし、幅も少し広げ、モデル名の表示行は省略せず2行まで折り返すようにしました
- クイックチャット画面で、モデル名とパラメータ調整コントロールが横並びで幅を取り合い、狭い場面で崩れる可能性があった問題を修正しました。モデル名を単独の行に分離しました

## [1.1.0] - 2026-08-18

UI、安定性、安全性の監査で見つかった問題をまとめて修正しました。

### セキュリティ

- OpenAI互換APIの`image_url`を検証するようになりました。ローカルパスと`file://`は拒否し、既定ではdata URI（base64）のみを受け付けます。従来はLAN公開時に、任意の読み取り可能な画像ファイルやMLXBarが到達できるURLをモデル経由で読み出せる可能性がありました
- 外部画像URLの取得を設定で明示的に有効にした場合でも、ループバック・プライベート・リンクローカルなどのアドレスへの取得は常に拒否し、リダイレクト追跡も行いません
- 受け付けた画像は権限0600の一時ディレクトリへ書き出し、生成終了時に削除します。Worker側へは常にそのパスのみを渡します
- launchdによる`/tmp`固定パスへのログ出力を廃止し、Coordinator自身が`Application Support`配下の権限0600のログへ書くようにしました
- ライブラリ検証の無効化とJIT許可をPythonランタイムの署名のみに限定し、SwiftUIアプリ本体はハードened runtimeの既定のまま署名するようにしました
- Workerのソケット権限を、シャットダウン時ではなく起動直後に0600へ設定するよう修正しました

### 安定性

- Coordinatorが異常終了した場合に、モデルを保持したままのWorkerプロセスが残る問題を修正しました。起動時に前回のプロセスを検出して停止し、残存ソケットも整理します
- SIGTERM／SIGINTを処理するようになり、ログアウトや`launchctl bootout`でもWorkerを残さず終了します
- Workerの標準エラー出力をログファイルへ切り替えました。従来はパイプが読み取られないまま埋まると、生成処理が無応答のまま停止する可能性がありました
- ランタイム更新にハードタイムアウトを追加しました（インストール1時間、その他10分）。停滞したダウンロードがジョブを無期限に「実行中」のまま残すことがなくなります
- アイドル中にWorkerが停止した場合（メモリ不足など）、状態表示が「ロード済み」のまま残らないようになりました
- 生成キャンセル記録の件数と保持期間に上限を設けました

### UI

- 言語切替が全画面で機能するようになりました。従来は翻訳データが参照されず、英語を選んでもボタンやメニューの大半が日本語のままでした
- Coordinatorへの管理API呼び出しにタイムアウトを追加しました。サービスが無応答になっても、操作ボタンが無効のまま復帰できなくなることはありません
- ストリーミング処理を非同期化し、標準エラー出力も同時に読み出すようにしました。長時間の生成中に他の画面更新が遅れる問題とデッドロックの可能性を解消しました
- 一時的な通信エラーの表示が、その後の再取得に成功しても残り続ける問題を修正しました
- 状態取得が二重に走った際、古い応答が新しい状態を上書きする問題を修正しました
- 別のファイル選択画面が開いている状態で選択操作をすると無反応だった問題を修正し、理由を表示するようにしました
- モデル一覧に読み込み中表示と、モデルが無い場合の案内を追加しました
- 接続URLボタンなどにVoiceOver向けの説明を追加しました

## [1.0.0] - 2026-08-11

最初の正式リリースです。

- 設定ウィンドウを可変サイズ化し、長いフォームが切れない最小サイズへ拡大
- GUIをEnglish標準に変更し、「General」から日本語へ切替可能に
- ランタイム操作をメニューバーから設定の「Runtime」へ移動
- 未導入の`mlx-lm`と`mlx-vlm`を初回起動時にバックグラウンドで自動インストール
- OpenAI互換APIの入力エラー形式、SSE終了処理、時刻整合性、接続維持を安定化

- Apple Silicon Mac向けメニューバー型モデル管理UI
- MLX LM、MLX VLM、LM Studioモデルの統合カタログ
- モデルのロード、生成、停止、アンロードと状態表示
- OpenAI Chat Completions互換APIとAPIキー・LAN公開設定
- ZCode、Open Interpreter、LibreChat向け互換処理
- tool calling、ストリーミング、並列要求キュー
- ランタイムのGUI更新、復元、旧版削除
- Max token、温度、Top P、繰り返しペナルティ設定
- APIログ、診断、データとランタイムの一括削除
